corpus: hugo
language: go
categories:
- foreign_import
- foreign_api
- foreign_concurrency
injection_hosts:
- pr: 0
  sha: a5ec54239312
fixtures:
- id: foreign_logrus_1
  file: breaks/break_foreign_logrus_1.go
  category: foreign_import
  hunk_start_line: 17
  hunk_end_line: 29
  rationale: 'github.com/sirupsen/logrus leveled/structured logger imported inside
    the hunk (logrus.New/JSONFormatter/WithFields) in the loggers package. Verified
    foreign at SHA: sirupsen/logrus = 0 git-grep hits in *.go and absent from go.mod;
    logging goes through the repo''s own common/loggers built on github.com/bep/logg,
    never a foreign leveled logger.'
  difficulty: easy
  host_file: common/loggers/logger.go
  host_inject_at_line: 30
- id: foreign_gin_1
  file: breaks/break_foreign_gin_1.go
  category: foreign_import
  hunk_start_line: 16
  hunk_end_line: 30
  rationale: 'github.com/gin-gonic/gin web framework imported inside the hunk
    (gin.Default/gin.Engine/gin.Context) to serve the live-reload preview, in the
    commands package. Verified foreign at SHA: gin-gonic/gin = 0 git-grep hits in
    *.go and absent from go.mod; Hugo''s dev server is the standard net/http server
    with its own handlers, never a foreign web framework.'
  difficulty: easy
  host_file: commands/server.go
  host_inject_at_line: 72
- id: foreign_redis_1
  file: breaks/break_foreign_redis_1.go
  category: foreign_import
  hunk_start_line: 15
  hunk_end_line: 28
  rationale: 'github.com/redis/go-redis/v9 client imported inside the hunk
    (redis.NewClient/redis.Options) to back the file cache with a remote store, in
    the filecache package. Verified foreign at SHA: redis/go-redis = 0 git-grep hits
    in *.go and absent from go.mod; Hugo''s caches persist to the local filesystem
    through github.com/spf13/afero, never a networked key-value store.'
  difficulty: easy
  host_file: cache/filecache/filecache.go
  host_inject_at_line: 37
- id: foreign_zap_1
  file: breaks/break_foreign_zap_1.go
  category: foreign_api
  hunk_start_line: 15
  hunk_end_line: 30
  rationale: 'go.uber.org/zap structured logger (zap.NewProduction/zap.String/zap.Int64)
    used to log each uploaded file, in the deploy package. Verified foreign at SHA:
    go.uber.org/zap = 0 git-grep hits in *.go and absent from go.mod (only the
    unrelated go.uber.org/automaxprocs appears); deploy progress is reported through
    the repo''s own common/loggers, never a foreign structured logger.'
  difficulty: easy
  host_file: deploy/deploy.go
  host_inject_at_line: 52
- id: foreign_gorm_1
  file: breaks/break_foreign_gorm_1.go
  category: foreign_api
  hunk_start_line: 12
  hunk_end_line: 36
  rationale: 'gorm.io/gorm ORM (gorm.Open/gorm.Model/gorm.Config + gorm.io/driver/sqlite)
    used to persist captured page metadata to a SQL database, in the hugolib package.
    Verified foreign at SHA: gorm.io/gorm and gorm.io/driver/sqlite = 0 git-grep hits
    in *.go and absent from go.mod; Hugo has no database layer — captured pages live
    in in-memory structures and the page bundle map, never an ORM.'
  difficulty: easy
  host_file: hugolib/pages_capture.go
  host_inject_at_line: 36
- id: foreign_prometheus_1
  file: breaks/break_foreign_prometheus_1.go
  category: foreign_api
  hunk_start_line: 12
  hunk_end_line: 29
  rationale: 'github.com/prometheus/client_golang metrics exporter
    (promauto.NewHistogramVec/prometheus.HistogramOpts) used to export build timings,
    in the metrics package. Verified foreign at SHA: prometheus/client_golang = 0
    git-grep hits in *.go and absent from go.mod; Hugo exposes timings through its
    own metrics.Provider (WriteMetrics to an io.Writer), never a metrics exporter.'
  difficulty: easy
  host_file: metrics/metrics.go
  host_inject_at_line: 32
- id: foreign_ants_1
  file: breaks/break_foreign_ants_1.go
  category: foreign_concurrency
  hunk_start_line: 19
  hunk_end_line: 44
  rationale: 'github.com/panjf2000/ants/v2 goroutine pool (ants.NewPool/pool.Submit/pool.Release)
    used to fan out tasks, in the common/para package. Verified foreign at SHA:
    panjf2000/ants = 0 git-grep hits in *.go and absent from go.mod; Hugo parallelises
    through its own common/para Workers over golang.org/x/sync/errgroup (17 errgroup
    sites), never a foreign goroutine-pool library.'
  difficulty: easy
  host_file: common/para/para.go
  host_inject_at_line: 22
- id: foreign_conc_1
  file: breaks/break_foreign_conc_1.go
  category: foreign_concurrency
  hunk_start_line: 18
  hunk_end_line: 35
  rationale: 'github.com/sourcegraph/conc structured-concurrency pool
    (pool.New().WithMaxGoroutines/p.Go/p.Wait) used to render sections concurrently,
    in the hugolib package. Verified foreign at SHA: sourcegraph/conc = 0 git-grep
    hits in *.go and absent from go.mod; Hugo renders in parallel through its own
    common/para Workers over golang.org/x/sync/errgroup, never a foreign
    structured-concurrency lib.'
  difficulty: easy
  host_file: hugolib/site.go
  host_inject_at_line: 93
- id: foreign_pond_1
  file: breaks/break_foreign_pond_1.go
  category: foreign_concurrency
  hunk_start_line: 15
  hunk_end_line: 32
  rationale: 'github.com/alitto/pond worker pool (pond.New/pool.Submit/pool.StopAndWait)
    used to resize images in parallel, in the resources package. Verified foreign at
    SHA: alitto/pond = 0 git-grep hits in *.go and absent from go.mod; Hugo processes
    images through its own resource cache and common/para Workers over
    golang.org/x/sync/errgroup, never a foreign worker-pool library.'
  difficulty: easy
  host_file: resources/image.go
  host_inject_at_line: 44
- id: foreign_zerolog_1
  file: breaks/break_foreign_zerolog_1.go
  category: foreign_import
  hunk_start_line: 13
  hunk_end_line: 25
  rationale: 'github.com/rs/zerolog leveled/structured logger imported inside the hunk
    (zerolog.New/With/Str/Logger) in the loggers package. Verified foreign at SHA:
    rs/zerolog = 0 git-grep hits in *.go and absent from go.mod; logging goes through
    the repo own common/loggers built on github.com/bep/logg, never a foreign leveled
    logger. Explicit foreign import in the hunk — the import stage catches this.'
  difficulty: easy
  host_file: common/loggers/logger.go
  host_inject_at_line: 159
- id: foreign_urfave_1
  file: breaks/break_foreign_urfave_1.go
  category: foreign_import
  hunk_start_line: 16
  hunk_end_line: 32
  rationale: 'github.com/urfave/cli/v2 app/command builder imported inside the hunk
    (cli.Command/cli.Context) in the commands package. Verified foreign at SHA:
    urfave/cli = 0 git-grep hits in *.go and absent from go.mod; Hugo CLI is built on
    github.com/spf13/cobra via github.com/bep/simplecobra, never a foreign command
    framework. Explicit foreign import in the hunk — the import stage catches this.'
  difficulty: easy
  host_file: commands/commandeer.go
  host_inject_at_line: 201
- id: foreign_resty_1
  file: breaks/break_foreign_resty_1.go
  category: foreign_api
  hunk_start_line: 13
  hunk_end_line: 28
  rationale: 'github.com/go-resty/resty/v2 HTTP client (resty.New/R/Get) used to fetch
    a remote resource, in the resource create package. Verified foreign at SHA:
    go-resty/resty = 0 git-grep hits in *.go and absent from go.mod; Hugo fetches
    remote resources through its own httpcache and the standard net/http client, never
    a foreign HTTP client library. Explicit foreign import in the hunk.'
  difficulty: easy
  host_file: resources/resource_factories/create/create.go
  host_inject_at_line: 180
- id: foreign_viper_1
  file: breaks/break_foreign_viper_1.go
  category: foreign_api
  hunk_start_line: 12
  hunk_end_line: 27
  rationale: 'github.com/spf13/viper config loader (viper.SetConfigName/AddConfigPath/
    ReadInConfig/AllSettings) used to load overrides, in the allconfig package.
    Verified foreign at SHA: spf13/viper = 0 git-grep hits in *.go and absent from
    go.mod (only other spf13/* — afero, cast, cobra, pflag, fsync — appear); Hugo
    decodes configuration through its own config package over spf13/afero and the
    pelletier/goccy decoders, never viper. Explicit foreign import in the hunk.'
  difficulty: easy
  host_file: config/allconfig/allconfig.go
  host_inject_at_line: 559
- id: foreign_workerpool_1
  file: breaks/break_foreign_workerpool_1.go
  category: foreign_concurrency
  hunk_start_line: 18
  hunk_end_line: 35
  rationale: 'github.com/gammazero/workerpool worker pool (workerpool.New/Submit/
    StopWait) used to fan out page rendering, in the hugolib package. Verified foreign
    at SHA: gammazero/workerpool = 0 git-grep hits in *.go and absent from go.mod;
    Hugo parallelises through its own common/para Workers over golang.org/x/sync/
    errgroup, never a foreign worker-pool library. Explicit foreign import in the hunk.'
  difficulty: easy
  host_file: hugolib/hugo_sites_build.go
  host_inject_at_line: 256
- id: foreign_msgpack_1
  file: breaks/break_foreign_msgpack_1.go
  category: foreign_import
  hunk_start_line: 12
  hunk_end_line: 23
  rationale: 'github.com/vmihailenco/msgpack/v5 imported under an alias (mp) inside the
    hunk and used as mp.Marshal to serialise a cache partition, in the dynacache
    package. Verified foreign at SHA: vmihailenco/msgpack = 0 git-grep hits in *.go and
    absent from go.mod; dynacache holds values in memory through github.com/bep/
    lazycache and on-disk encoding goes through the repo tmc/gob paths, never a foreign
    msgpack codec. Medium: the import is aliased (mp) rather than bare, stressing path
    resolution — the Go import stage still extracts the full path, so it catches.'
  difficulty: medium
  host_file: cache/dynacache/dynacache.go
  host_inject_at_line: 143
- id: foreign_badger_1
  file: breaks/break_foreign_badger_1.go
  category: foreign_import
  hunk_start_line: 12
  hunk_end_line: 25
  rationale: 'github.com/dgraph-io/badger/v4 embedded LSM key-value store imported
    inside the hunk together with its badger/v4/options submodule (badger.Open/
    Defaultoptions/options.ZSTD) to back the file cache, in the filecache package.
    Verified foreign at SHA: dgraph-io/badger = 0 git-grep hits in *.go and absent from
    go.mod; Hugo caches persist to the local filesystem through github.com/spf13/afero,
    never an embedded database engine. Medium: a submodule import (badger/v4/options)
    stresses path resolution — the Go import stage extracts the full path, so it catches.'
  difficulty: medium
  host_file: cache/filecache/filecache.go
  host_inject_at_line: 184
- id: foreign_jsonparser_1
  file: breaks/break_foreign_jsonparser_1.go
  category: foreign_api
  hunk_start_line: 13
  hunk_end_line: 36
  rationale: 'github.com/buger/jsonparser reached via package-qualified calls
    (jsonparser.ObjectEach/ArrayEach/GetUnsafeString) to walk a JSON blob, in the
    metadecoders package. Verified foreign at SHA: buger/jsonparser = 0 git-grep hits
    in *.go and absent from go.mod; front-matter and data files are decoded through the
    repo parser/metadecoders over goccy/go-yaml and encoding/json, never a zero-alloc
    third-party JSON walker. Medium: no import line inside the hunk (assumed in the
    decoy import block), so the only tell is the foreign callee — this stresses
    call_receiver, not the import tripwire. ObjectEach/ArrayEach/GetUnsafeString are all
    0-attested as repo method names, so the three package-qualified calls fire.'
  difficulty: medium
  host_file: parser/metadecoders/decoder.go
  host_inject_at_line: 184
- id: foreign_gjson_1
  file: breaks/break_foreign_gjson_1.go
  category: foreign_api
  hunk_start_line: 12
  hunk_end_line: 29
  rationale: 'github.com/tidwall/gjson reached via package-qualified calls
    (gjson.ValidBytes/GetManyBytes/GetMany/ForEachLine) to query a JSON blob, in the
    metadecoders package. Verified foreign at SHA: tidwall/gjson = 0 git-grep hits in
    *.go and absent from go.mod; data files are decoded through the repo
    parser/metadecoders and encoding/json, never a foreign JSON-path query library.
    Medium: no import line inside the hunk (assumed in the decoy import block), so the
    tell is the foreign callee — stresses call_receiver. ValidBytes/GetManyBytes/
    GetMany/ForEachLine are all 0-attested as repo method names; the four
    package-qualified calls together clear the call_receiver threshold (three alone
    landed just under — a genuine medium boundary).'
  difficulty: medium
  host_file: parser/metadecoders/decoder.go
  host_inject_at_line: 117
- id: foreign_tunny_1
  file: breaks/break_foreign_tunny_1.go
  category: foreign_concurrency
  hunk_start_line: 18
  hunk_end_line: 36
  rationale: 'github.com/Jeffail/tunny reached via a package-qualified constructor
    (tunny.NewFunc) to build a fixed worker pool for image resizing, in the images
    package. Verified foreign at SHA: Jeffail/tunny = 0 git-grep hits in *.go and
    absent from go.mod; image work is fanned out through the repo common/para Workers
    over golang.org/x/sync/errgroup, never a foreign goroutine-pool library. Medium:
    no import line inside the hunk (assumed in the decoy import block); the pool
    methods (Process/Close) go through a local receiver, so the tell is the one
    package-qualified callee. NewFunc is 0-attested as a repo method name, so it
    should fire.'
  difficulty: medium
  host_file: resources/images/image.go
  host_inject_at_line: 94
- id: foreign_validator_1
  file: breaks/break_foreign_validator_1.go
  category: foreign_api
  hunk_start_line: 12
  hunk_end_line: 26
  rationale: 'github.com/go-playground/validator/v10 reached through a receiver
    variable — validator.New() binds a foreign *Validate, then v.Struct(...) does the
    work, in the allconfig package. Verified foreign at SHA: go-playground/validator =
    0 git-grep hits in *.go and absent from go.mod; configuration is validated by the
    repo own CompileConfig checks, never a struct-tag validation library. HARD (may
    miss): the constructor leaf method New collides with the repo pervasive New()
    (1350 call sites) so it is method-attested, and every other call goes through the
    local receiver v — no callee names a foreign namespace. A genuinely masked foreign
    API; a miss here is an honest finding, not a fixture to soften.'
  difficulty: hard
  host_file: config/allconfig/configlanguage.go
  host_inject_at_line: 51
- id: foreign_gocache_1
  file: breaks/break_foreign_gocache_1.go
  category: foreign_api
  hunk_start_line: 15
  hunk_end_line: 27
  rationale: 'github.com/patrickmn/go-cache (aliased gocache) reached through a
    receiver variable — gocache.New(...) binds a foreign *Cache, then store.Set(...)/
    store.Get(...) do the work, in the dynacache package. Verified foreign at SHA:
    patrickmn/go-cache = 0 git-grep hits in *.go and absent from go.mod; dynacache
    holds values in memory through github.com/bep/lazycache, never a foreign TTL cache.
    HARD (may miss): the constructor leaf method New collides with the repo pervasive
    New() and the value methods Set/Get collide with attested repo methods (258/211
    call sites) while going through the local receiver store — no callee names a
    foreign namespace. A genuinely masked foreign API; a miss is an honest finding.'
  difficulty: hard
  host_file: cache/dynacache/dynacache.go
  host_inject_at_line: 180
- id: foreign_semaphore_1
  file: breaks/break_foreign_semaphore_1.go
  category: foreign_concurrency
  hunk_start_line: 18
  hunk_end_line: 40
  rationale: 'github.com/marusama/semaphore/v2 reached through a receiver variable —
    semaphore.New(...) binds a foreign Semaphore, then sem.Acquire(...)/sem.Release(...)
    bound parallelism, in the hugolib package. Verified foreign at SHA:
    marusama/semaphore = 0 git-grep hits in *.go and absent from go.mod (golang.org/x/
    sync is present but only errgroup is used, never x/sync/semaphore); Hugo bounds
    parallelism through its own common/para Workers over golang.org/x/sync/errgroup,
    never a foreign weighted-semaphore library. HARD (may miss): the constructor leaf
    method New collides with the repo pervasive New() and Acquire/Release go through
    the local receiver sem — no callee names a foreign namespace. A genuinely masked
    foreign primitive; a miss is an honest finding.'
  difficulty: hard
  host_file: hugolib/site.go
  host_inject_at_line: 193
