corpus: ripgrep
language: rust
categories:
- wrong_error_discipline
- wrong_concurrency
- wrong_api_within_known_lib
- naming_shape_break
- foreign_import
- foreign_api
- foreign_concurrency
injection_hosts:
- pr: 0
  sha: 48b0c795f4feb37343b2832d991c5c6a3900c08a
fixtures:
- id: err_unwrap_chain_1
  file: breaks/break_err_unwrap_chain_1.rs
  category: wrong_error_discipline
  class: semantic_convention
  hunk_start_line: 32
  hunk_end_line: 44
  rationale: 'File::open().expect() + metadata().unwrap() + read_to_end().expect()
    chain in a library search path. Verified at SHA: grep-searcher src is
    Result-everywhere (io::Result / Sink S::Error); unwrap/expect on I/O confined
    to tests and testutil.rs (panic! hits in crates/searcher/src are macros.rs
    doc-macro and testutil.rs only).'
  difficulty: medium
  host_file: crates/searcher/src/searcher/glue.rs
  host_inject_at_line: 148
- id: err_process_exit_1
  file: breaks/break_err_process_exit_1.rs
  category: wrong_error_discipline
  class: semantic_convention
  hunk_start_line: 27
  hunk_end_line: 45
  rationale: 'eprintln! + std::process::exit(2) deep in walker library code.
    Verified at SHA: git grep process::exit hits only crates/core/messages.rs
    (binary-side err_exit paths) and crates/{grep,searcher}/examples; the ignore
    crate reports failures via its own Error enum (crates/ignore/src/lib.rs:66)
    and never exits.'
  difficulty: easy
  host_file: crates/ignore/src/walk.rs
  host_inject_at_line: 508
- id: err_swallowed_result_1
  file: breaks/break_err_swallowed_result_1.rs
  category: wrong_error_discipline
  class: semantic_convention
  hunk_start_line: 31
  hunk_end_line: 47
  rationale: 'let _ = wtr.write_all(...) and .flush().ok() swallowing io::Results
    on a printer write path returning (). Verified at SHA: every write helper in
    crates/printer/src returns io::Result and propagates with ?; `let _ =`
    appears exactly 3 times in production src, all in crates/core/main.rs at
    process exit (lines 148/225/226), never in the printer.'
  difficulty: hard
  host_file: crates/printer/src/standard.rs
  host_inject_at_line: 596
- id: err_panic_glob_1
  file: breaks/break_err_panic_glob_1.rs
  category: wrong_error_discipline
  class: semantic_convention
  hunk_start_line: 26
  hunk_end_line: 55
  rationale: 'panic!("invalid glob: ...") + expect() on user-supplied pattern
    input in a glob parser. Verified at SHA: globset reports malformed patterns
    through its custom Error + ErrorKind (crates/globset/src/lib.rs:167, Error
    impl at :198); no panic! on input anywhere in crates/globset/src.'
  difficulty: medium
  host_file: crates/globset/src/glob.rs
  host_inject_at_line: 122
- id: conc_mutex_threads_1
  file: breaks/break_conc_mutex_threads_1.rs
  category: wrong_concurrency
  class: semantic_convention
  hunk_start_line: 30
  hunk_end_line: 53
  rationale: 'Raw thread::spawn worker fan-out popping an Arc<Mutex<Vec>> queue
    and pushing into an Arc<Mutex<Vec>> accumulator. Verified at SHA: the repo
    parallel walker is crossbeam-deque work stealing behind WalkParallel
    (crates/ignore/src/walk.rs); Arc<Mutex<Vec>> accumulation appears only in
    walk.rs tests (line 2116); production thread::spawn is limited to single
    helper threads in cli/process.rs and core/main.rs.'
  difficulty: medium
  host_file: crates/ignore/src/dir.rs
  host_inject_at_line: 614
- id: conc_busywait_1
  file: breaks/break_conc_busywait_1.rs
  category: wrong_concurrency
  class: semantic_convention
  hunk_start_line: 30
  hunk_end_line: 53
  rationale: 'Busy-wait polling: thread::sleep(1ms) loop on an AtomicBool done
    flag over a hand-rolled Mutex<VecDeque> queue. Verified at SHA: zero
    thread::sleep in production src (git grep thread::sleep over crates/*/src
    and crates/core is empty); cross-thread handoff uses crossbeam-channel
    (core/main.rs print thread) or the crossbeam-deque walker.'
  difficulty: medium
  host_file: crates/core/search.rs
  host_inject_at_line: 198
- id: api_regex_in_loop_1
  file: breaks/break_api_regex_in_loop_1.rs
  category: wrong_api_within_known_lib
  class: semantic_convention
  hunk_start_line: 26
  hunk_end_line: 44
  rationale: 'regex::Regex::new(&pat).unwrap() recompiled for every haystack line
    inside the match loop. Verified at SHA: regex 1.x is in-workspace (Cargo.lock
    v1.12.2; dev-dependency of grep-matcher/grep-searcher) but the production
    match path builds one matcher up front via grep::regex::RegexMatcherBuilder
    (crates/core/flags/hiargs.rs:462) over regex-automata; nothing compiles a
    pattern per line.'
  difficulty: medium
  host_file: crates/regex/src/matcher.rs
  host_inject_at_line: 405
- id: api_fs_read_dir_1
  file: breaks/break_api_fs_read_dir_1.rs
  category: wrong_api_within_known_lib
  class: semantic_convention
  hunk_start_line: 28
  hunk_end_line: 47
  rationale: 'Hand-rolled recursive std::fs::read_dir traversal with a string
    startswith(".") hidden check, duplicating the repo-owned ignore crate.
    Verified at SHA: zero read_dir in production src; traversal is
    ignore::WalkBuilder (used in crates/core/flags/hiargs.rs, defined in
    crates/ignore/src/walk.rs) which owns hidden/gitignore filtering.'
  difficulty: medium
  host_file: crates/core/haystack.rs
  host_inject_at_line: 102
- id: api_println_output_1
  file: breaks/break_api_println_output_1.rs
  category: wrong_api_within_known_lib
  class: semantic_convention
  hunk_start_line: 29
  hunk_end_line: 39
  rationale: 'println!-driven match and summary output. Verified at SHA: zero
    println! in crates/core src (git grep empty); all match output flows through
    the repo-owned grep-printer Standard/Summary printers over
    termcolor::WriteColor buffers (WriteColor across crates/printer/src, e.g.
    counter.rs, path.rs, hyperlink/mod.rs).'
  difficulty: easy
  host_file: crates/printer/src/summary.rs
  host_inject_at_line: 368
- id: api_read_to_string_1
  file: breaks/break_api_read_to_string_1.rs
  category: wrong_api_within_known_lib
  class: semantic_convention
  hunk_start_line: 28
  hunk_end_line: 43
  rationale: 'fs::read_to_string whole-file slurp + lines().contains() scan,
    hand-rolling what grep-searcher provides. Verified at SHA: the searcher is
    memmap2-backed or incremental with encoding_rs transcoding and binary
    detection (crates/searcher/Cargo.toml deps memmap2/encoding_rs/
    encoding_rs_io), driven through Matcher + Sink; no String slurp search
    exists in production src.'
  difficulty: hard
  host_file: crates/searcher/src/searcher/mod.rs
  host_inject_at_line: 301
- id: name_camelcase_1
  file: breaks/break_name_camelcase_1.rs
  category: naming_shape_break
  hunk_start_line: 29
  hunk_end_line: 58
  rationale: 'camelCase fields/methods (caseInsensitive, setCaseInsensitive,
    getMatchCount) and Java get/set accessor shape. Verified at SHA: the repo is
    uniformly snake_case with fluent builder setters taking `yes: bool` and
    returning &mut Self (GlobBuilder::case_insensitive in
    crates/globset/src/glob.rs, SearcherBuilder in crates/searcher); no
    getFoo/setFoo accessors in src.'
  difficulty: easy
  host_file: crates/globset/src/lib.rs
  host_inject_at_line: 253
- id: name_screaming_locals_1
  file: breaks/break_name_screaming_locals_1.rs
  category: naming_shape_break
  hunk_start_line: 27
  hunk_end_line: 40
  rationale: 'SCREAMING_SNAKE locals (N_TOTAL, U_MAX_OFFSET, STR_LABEL) plus
    Hungarian prefixes (strHaystackName, uOffsets) in a printer helper. Verified
    at SHA: locals in crates/printer/src are lower snake_case throughout;
    upper-case identifiers are reserved for consts/statics; no type-prefixed
    identifiers in src.'
  difficulty: medium
  host_file: crates/printer/src/util.rs
  host_inject_at_line: 366
- id: foreign_rayon_1
  file: breaks/break_foreign_rayon_1.rs
  category: foreign_import
  hunk_start_line: 33
  hunk_end_line: 45
  rationale: 'use rayon::prelude::* + par_iter() parallel rule matching, import
    inside hunk. Verified foreign at SHA: `name = "rayon"` has zero occurrences
    in Cargo.lock (and no Cargo.toml lists it); the repo''s only parallelism is
    crossbeam-channel plus its own crossbeam-deque work-stealing walker in
    crates/ignore/src/walk.rs.'
  difficulty: easy
  host_file: crates/ignore/src/gitignore.rs
  host_inject_at_line: 316
- id: foreign_tokio_1
  file: breaks/break_foreign_tokio_1.rs
  category: foreign_api
  hunk_start_line: 34
  hunk_end_line: 45
  rationale: 'tokio::runtime::Runtime::new() + .block_on() spun up inside a
    synchronous per-file match-counting helper, referenced by fully-qualified
    path (no `use` import). Verified foreign at SHA: `tokio` = 0 grep hits in
    crates/ (Cargo.lock lists no tokio package at all); `Runtime::new` and
    `block_on` = 0 src hits (non-colliding); the searcher''s Core/MultiLine
    sink is plain synchronous Rust over Matcher + Sink, never async.'
  difficulty: easy
  host_file: crates/searcher/src/searcher/glue.rs
  host_inject_at_line: 148
- id: foreign_reqwest_1
  file: breaks/break_foreign_reqwest_1.rs
  category: foreign_api
  hunk_start_line: 37
  hunk_end_line: 43
  rationale: 'reqwest::blocking::get() fetching a shared ignore-rule list over
    HTTP before the walker builds its ignore stack, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA:
    `reqwest` = 0 grep hits in crates/ (Cargo.lock lists no reqwest package);
    `blocking::get` = 0 src hits (non-colliding); the ignore crate resolves
    rules exclusively from the local filesystem (.gitignore/.ignore/
    .rgignore) via its own Gitignore builder, never over the network.'
  difficulty: easy
  host_file: crates/ignore/src/walk.rs
  host_inject_at_line: 508
- id: foreign_diesel_1
  file: breaks/break_foreign_diesel_1.rs
  category: foreign_api
  hunk_start_line: 37
  hunk_end_line: 53
  rationale: 'diesel::pg::PgConnection::establish() + sql_query().bind().execute()
    persisting formatted match records to Postgres, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA: `diesel`
    = 0 grep hits in crates/ (Cargo.lock lists no diesel package);
    `PgConnection` and `establish_connection`/`.establish(` = 0 src hits
    (non-colliding); the standard printer only ever writes to its generic
    `W: WriteColor` writer over termcolor, never to a database.'
  difficulty: easy
  host_file: crates/printer/src/standard.rs
  host_inject_at_line: 596
- id: foreign_sqlx_1
  file: breaks/break_foreign_sqlx_1.rs
  category: foreign_api
  hunk_start_line: 24
  hunk_end_line: 40
  rationale: 'sqlx::postgres::PgPoolOptions + sqlx::query().bind().execute()
    async-logging per-run match totals to Postgres, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA: `sqlx` =
    0 grep hits in crates/ (Cargo.lock lists no sqlx package); `PgPoolOptions`
    and `sqlx::query`/`fetch_all` = 0 src hits (non-colliding); the standard
    printer''s stats are held in-process via this crate''s own counter types
    and written to the terminal writer, never queried against or persisted to
    a database.'
  difficulty: easy
  host_file: crates/printer/src/standard.rs
  host_inject_at_line: 596
- id: foreign_tracing_1
  file: breaks/break_foreign_tracing_1.rs
  category: foreign_import
  hunk_start_line: 26
  hunk_end_line: 40
  rationale: 'use tracing::instrument + tracing::info_span! / tracing::debug!
    spans wrapped around the searcher core''s match loop, import inside hunk.
    Verified foreign at SHA 48b0c795f4fe: `tracing` = 0 grep hits across *.rs
    and absent from every Cargo.toml; ripgrep''s diagnostics go through the
    `log` crate macros (log:: = 84 src hits, e.g. log::debug!/log::trace!) and
    crates/core/messages.rs, never a tracing subscriber or spans.'
  difficulty: easy
  host_file: crates/searcher/src/searcher/core.rs
  host_inject_at_line: 38
- id: foreign_rusqlite_1
  file: breaks/break_foreign_rusqlite_1.rs
  category: foreign_api
  hunk_start_line: 25
  hunk_end_line: 38
  rationale: 'rusqlite::Connection::open() + conn.execute() with rusqlite::params!
    persisting formatted match records to a local SQLite database, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA 48b0c795f4fe:
    `rusqlite` = 0 grep hits across *.rs and absent from every Cargo.toml;
    `Connection::open` = 0 src hits (non-colliding); the JSON printer serialises
    each match to its `W: io::Write` sink via serde_json (crates/printer/src/
    json.rs), never to a SQL database.'
  difficulty: easy
  host_file: crates/printer/src/json.rs
  host_inject_at_line: 492
- id: foreign_threadpool_1
  file: breaks/break_foreign_threadpool_1.rs
  category: foreign_concurrency
  hunk_start_line: 26
  hunk_end_line: 38
  rationale: 'threadpool::ThreadPool::new() + pool.execute() fanning per-file
    search across a foreign thread pool, referenced by fully-qualified path (no
    `use` import). Verified foreign at SHA 48b0c795f4fe: `threadpool` = 0 grep
    hits across *.rs and absent from every Cargo.toml; `ThreadPool` = 0 src hits
    (non-colliding); ripgrep''s only parallelism is its own crossbeam-deque
    work-stealing walker behind WalkParallel (crates/ignore/src/walk.rs) plus
    crossbeam-channel, never an external thread pool.'
  difficulty: easy
  host_file: crates/core/search.rs
  host_inject_at_line: 242
- id: foreign_smol_1
  file: breaks/break_foreign_smol_1.rs
  category: foreign_concurrency
  hunk_start_line: 24
  hunk_end_line: 38
  rationale: 'smol::block_on() + smol::spawn() driving the slice-by-line sink
    concurrently on a foreign async runtime, referenced by fully-qualified path
    (no `use` import). Verified foreign at SHA 48b0c795f4fe: `smol` = 0 grep hits
    across *.rs and absent from every Cargo.toml; `block_on` = 0 src hits
    (non-colliding); the searcher drives its Matcher + Sink synchronously
    (ReadByLine/SliceByLine glue over a plain Core in crates/searcher/src/
    searcher/glue.rs), with no async runtime anywhere in the crate.'
  difficulty: easy
  host_file: crates/searcher/src/searcher/glue.rs
  host_inject_at_line: 101
- id: foreign_clap_1
  file: breaks/break_foreign_clap_1.rs
  category: foreign_import
  hunk_start_line: 24
  hunk_end_line: 39
  rationale: 'use clap::Parser + #[derive(Parser)] CLI arg struct standing in for
    ripgrep''s hand-rolled flag layer, import inside hunk. Verified foreign at SHA
    48b0c795f4fe: `clap` = 0 grep hits across *.rs and absent from every
    Cargo.toml/Cargo.lock; ripgrep parses argv itself through crates/core/flags
    (lowargs/hiargs over the `lexopt` crate), never a derive-macro parser.'
  difficulty: easy
  host_file: crates/core/flags/config.rs
  host_inject_at_line: 54
- id: foreign_ron_1
  file: breaks/break_foreign_ron_1.rs
  category: foreign_import
  hunk_start_line: 24
  hunk_end_line: 33
  rationale: 'use ron::de::from_reader deserializing a shared ignore-rule file from
    Rusty Object Notation, import inside hunk. Verified foreign at SHA 48b0c795f4fe:
    `ron` = 0 grep hits across *.rs and absent from every Cargo.toml/Cargo.lock;
    ripgrep resolves ignore rules from plain-text .gitignore/.ignore/.rgignore
    files via its own Gitignore builder, never a structured RON document.'
  difficulty: easy
  host_file: crates/ignore/src/gitignore.rs
  host_inject_at_line: 316
- id: foreign_serde_yaml_1
  file: breaks/break_foreign_serde_yaml_1.rs
  category: foreign_import
  hunk_start_line: 24
  hunk_end_line: 32
  rationale: 'use serde_yaml + serde_yaml::from_str deserializing a user file-type
    map from a YAML document, import inside hunk. Verified foreign at SHA
    48b0c795f4fe: `serde_yaml` = 0 grep hits across *.rs and absent from every
    Cargo.toml/Cargo.lock; ripgrep''s file-type definitions are compiled-in Rust
    tables (ignore/src/default_types.rs) extended via `--type-add` name:glob
    strings, never parsed from a YAML config.'
  difficulty: easy
  host_file: crates/ignore/src/types.rs
  host_inject_at_line: 182
- id: foreign_mongodb_1
  file: breaks/break_foreign_mongodb_1.rs
  category: foreign_api
  hunk_start_line: 23
  hunk_end_line: 29
  rationale: 'mongodb::sync::Client::with_uri_str() + collection().insert_one()
    persisting each formatted match to a document store, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA 48b0c795f4fe:
    `mongodb` = 0 grep hits across *.rs and absent from every Cargo.toml/Cargo.lock;
    `with_uri_str` = 0 src hits (non-colliding); the JSON printer serialises each
    match to its `W: io::Write` sink via serde_json (crates/printer/src/json.rs),
    never to a database.'
  difficulty: easy
  host_file: crates/printer/src/json.rs
  host_inject_at_line: 492
- id: foreign_flume_1
  file: breaks/break_foreign_flume_1.rs
  category: foreign_concurrency
  hunk_start_line: 25
  hunk_end_line: 35
  rationale: 'flume::unbounded() MPMC channel fanning per-file outcomes back from
    worker threads, referenced by fully-qualified path (no `use` import). Verified
    foreign at SHA 48b0c795f4fe: `flume` = 0 grep hits across *.rs and absent from
    every Cargo.toml/Cargo.lock; ripgrep''s cross-thread handoff is
    crossbeam-channel (core/main.rs print thread) over its own crossbeam-deque
    work-stealing walker, never a flume channel.'
  difficulty: easy
  host_file: crates/core/search.rs
  host_inject_at_line: 242
- id: foreign_parking_lot_1
  file: breaks/break_foreign_parking_lot_1.rs
  category: foreign_concurrency
  hunk_start_line: 25
  hunk_end_line: 34
  rationale: 'use parking_lot::Mutex as PlMutex (aliased import) guarding the shared
    walker visit state across worker threads. Verified foreign at SHA 48b0c795f4fe:
    `parking_lot` = 0 grep hits across *.rs and absent from every
    Cargo.toml/Cargo.lock; the parallel walker coordinates through crossbeam (its
    crossbeam-deque work-stealing stack behind WalkParallel), using std::sync::Mutex
    where it needs a lock, never parking_lot.'
  difficulty: medium
  host_file: crates/ignore/src/dir.rs
  host_inject_at_line: 614
- id: foreign_jwalk_1
  file: breaks/break_foreign_jwalk_1.rs
  category: foreign_import
  hunk_start_line: 24
  hunk_end_line: 37
  rationale: 'use jwalk::WalkDir + use jwalk::rayon::prelude::* (submodule imports)
    replacing the repo-owned ignore::WalkBuilder traversal with a foreign parallel
    walker. Verified foreign at SHA 48b0c795f4fe: `jwalk` = 0 grep hits across *.rs
    and absent from every Cargo.toml/Cargo.lock; ripgrep''s recursive traversal is
    its own crossbeam-deque work-stealing WalkParallel over the `ignore` crate
    (which owns gitignore/hidden filtering), never an external walker.'
  difficulty: medium
  host_file: crates/ignore/src/walk.rs
  host_inject_at_line: 508
- id: foreign_indicatif_1
  file: breaks/break_foreign_indicatif_1.rs
  category: foreign_api
  hunk_start_line: 24
  hunk_end_line: 33
  rationale: 'indicatif::ProgressBar::new() reached through a receiver variable
    (pb.inc / pb.finish_and_clear), built by a fully-qualified constructor (no
    `use` import). Verified foreign at SHA 48b0c795f4fe: `indicatif` = 0 grep hits
    across *.rs and absent from every Cargo.toml/Cargo.lock; ripgrep prints no
    progress UI — results stream straight through the grep-printer onto the
    termcolor writer, with no terminal spinner or bar.'
  difficulty: medium
  host_file: crates/core/search.rs
  host_inject_at_line: 198
- id: foreign_prettytable_1
  file: breaks/break_foreign_prettytable_1.rs
  category: foreign_api
  hunk_start_line: 22
  hunk_end_line: 29
  rationale: 'prettytable::Table::new() reached through a receiver variable
    (table.add_row / table.to_string) with the prettytable::row! macro, built by a
    fully-qualified constructor (no `use` import). Verified foreign at SHA
    48b0c795f4fe: `prettytable` = 0 grep hits across *.rs and absent from every
    Cargo.toml/Cargo.lock; the summary printer writes counts line-by-line to its
    termcolor WriteColor sink (crates/printer/src/summary.rs), never a bordered
    table.'
  difficulty: medium
  host_file: crates/printer/src/summary.rs
  host_inject_at_line: 368
- id: foreign_async_std_1
  file: breaks/break_foreign_async_std_1.rs
  category: foreign_concurrency
  hunk_start_line: 29
  hunk_end_line: 43
  rationale: 'async-std runtime driving the slice-by-line sink concurrently via a
    bare `task::block_on` / `task::spawn` callee, its `use async_std::task` import
    intentionally in the decoy region above the scored hunk (bare-callee variant).
    Verified foreign at SHA 48b0c795f4fe: `async_std` = 0 grep hits across *.rs and
    absent from every Cargo.toml/Cargo.lock; the searcher drives its Matcher + Sink
    synchronously (ReadByLine/SliceByLine glue over a plain Core), with no async
    runtime.'
  difficulty: medium
  host_file: crates/searcher/src/searcher/glue.rs
  host_inject_at_line: 101
- id: foreign_itertools_1
  file: breaks/break_foreign_itertools_1.rs
  category: foreign_api
  hunk_start_line: 28
  hunk_end_line: 35
  rationale: 'HARD (masked): itertools adaptor methods (.tuple_windows / .dedup_by)
    reached as extension methods on a local iterator receiver — no
    `use itertools::Itertools`, no `itertools::` path, so the only tell is the
    method names and call-receiver cannot fire (local receiver). Verified foreign
    at SHA 48b0c795f4fe: `itertools` = 0 grep hits across *.rs and absent from every
    Cargo.toml/Cargo.lock; `tuple_windows` = 0 src hits; ripgrep composes iterators
    with std combinators and its own helpers, never the itertools extension trait.'
  difficulty: hard
  host_file: crates/printer/src/util.rs
  host_inject_at_line: 366
- id: foreign_dashmap_1
  file: breaks/break_foreign_dashmap_1.rs
  category: foreign_concurrency
  hunk_start_line: 30
  hunk_end_line: 39
  rationale: 'HARD (masked): dashmap concurrent map named only as a
    `&dashmap::DashMap` parameter type (never a `use`, never a call callee) and
    reached through a local receiver with attested method names (.entry/.insert),
    so call-receiver stays quiet and only token surprise on dashmap/DashMap could
    fire. Verified foreign at SHA 48b0c795f4fe: `dashmap` = 0 grep hits across *.rs
    and absent from every Cargo.toml/Cargo.lock; ripgrep aggregates per-thread
    stats into its own Stats type and merges them, never a shared concurrent map.'
  difficulty: hard
  host_file: crates/core/search.rs
  host_inject_at_line: 242
- id: foreign_redis_1
  file: breaks/break_foreign_redis_1.rs
  category: foreign_api
  hunk_start_line: 30
  hunk_end_line: 37
  rationale: 'HARD (masked, leaf collision): redis client named only as a
    `&mut redis::Connection` parameter type and reached through a local receiver
    whose leaf methods (.get/.set) collide with attested repo methods — so
    call-receiver''s method-attested guard suppresses it and only token surprise on
    redis/Connection could fire. Verified foreign at SHA 48b0c795f4fe: `redis` = 0
    grep hits across *.rs and absent from every Cargo.toml/Cargo.lock; the standard
    printer writes rendered lines straight to its `W: WriteColor` termcolor sink,
    never to a cache.'
  difficulty: hard
  host_file: crates/printer/src/standard.rs
  host_inject_at_line: 596
