# ESLint's docs/ is a standalone Eleventy site with its own dependencies
# (js-yaml, etc.) — peripheral tooling, not the linter's authored library voice.
# A real setup excludes it; the fit corpus should be lib/ + packages/, not the
# marketing/docs site.
docs/
