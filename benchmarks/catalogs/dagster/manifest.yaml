corpus: dagster
language: multi
categories:
  # Python categories
  - framework_swap
  - asset_definition
  - scheduling
  - validation
  - exception_handling
  # TypeScript categories
  - state_management
  - data_fetching
  - routing
  - styling
  # RUBRIC v2 gated novel-pattern classes (foreign-dependency breaks)
  - foreign_import
  - foreign_api
  - foreign_concurrency
injection_hosts:
  - pr: 0
    sha: 549b894e4ba03d5836459d58932105b77fafe242
fixtures:
  # ---------------------------------------------------------------------
  # Python — Dagster vs other-framework paradigms
  # ---------------------------------------------------------------------
  - id: dagster_py_framework_swap_2
    language: python
    file: breaks/py/paradigm_break_prefect_task.py
    category: framework_swap
    class: foreign_api
    hunk_start_line: 14
    hunk_end_line: 53
    rationale: "Dagster uses @op / @asset with RetryPolicy objects; Prefect's @task(retries=3,
      retry_delay_seconds=exponential_backoff(...)), @flow, PrefectFuture, and task_runner
      parameters are entirely absent from the Dagster corpus"
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/op_definition.py
    host_inject_at_line: 350

  - id: dagster_py_framework_swap_3
    language: python
    file: breaks/py/paradigm_break_luigi_pipeline.py
    category: framework_swap
    class: foreign_api
    hunk_start_line: 15
    hunk_end_line: 79
    rationale: "Dagster expresses asset dependencies via parameter names or deps=[]; Luigi's class-based
      Task subclasses with requires() / output() / run() methods, luigi.LocalTarget, luigi.DateParameter,
      self.input().open(), and luigi.build() are absent from the Dagster corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/job_definition.py
    host_inject_at_line: 600

  - id: dagster_py_asset_definition_1
    language: python
    file: breaks/py/paradigm_break_procedural_etl.py
    category: asset_definition
    class: semantic_convention
    hunk_start_line: 14
    hunk_end_line: 72
    rationale: "Dagster models data as @asset-decorated functions with return values persisted by an
      IOManager; this file uses plain module-level functions that mutate a shared _state dict and call
      each step explicitly in run_pipeline() — @asset, AssetIn, IOManager, Definitions, and
      MaterializeResult are absent from the corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/definitions_class.py
    host_inject_at_line: 500

  - id: dagster_py_asset_definition_2
    language: python
    file: breaks/py/paradigm_break_class_pipeline.py
    category: asset_definition
    class: semantic_convention
    hunk_start_line: 13
    hunk_end_line: 76
    rationale: "Dagster models data with @asset-decorated pure functions wired by the asset graph;
      a Pipeline class that stores intermediate data as self.raw_rows / self.clean_rows and calls
      connect() / extract() / transform() / load() in sequence replaces the entire @asset /
      Definitions vocabulary absent from the corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/definitions_class.py
    host_inject_at_line: 800

  - id: dagster_py_scheduling_1
    language: python
    file: breaks/py/paradigm_break_apscheduler.py
    category: scheduling
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 76
    rationale: "Dagster declares schedules as ScheduleDefinition objects registered in Definitions()
      with cron_schedule, execution_timezone, and DefaultScheduleStatus managed by the Dagster daemon;
      APScheduler's BlockingScheduler, scheduler.add_job(), CronTrigger, and replace_existing=True
      are absent from the Dagster corpus"
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/schedule_definition.py
    host_inject_at_line: 650

  - id: dagster_py_scheduling_2
    language: python
    file: breaks/py/paradigm_break_cron_schedule.py
    category: scheduling
    class: semantic_convention
    hunk_start_line: 14
    hunk_end_line: 75
    rationale: "Dagster uses ScheduleDefinition with execution_fn yielding RunRequest objects evaluated
      by the Dagster daemon; croniter for next-fire computation, threading.Event for sleep, and
      a manual _schedule_loop() with try/except are absent from the Dagster corpus — no RunRequest,
      cron_schedule field, or job_name appear"
    difficulty: hard
    host_file: python_modules/dagster/dagster/_core/definitions/schedule_definition.py
    host_inject_at_line: 950

  - id: dagster_py_validation_1
    language: python
    file: breaks/py/paradigm_break_jsonschema_validation.py
    category: validation
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 83
    rationale: "Dagster validates run config through dagster.Field / dagster.Shape / dagster.Array
      with DagsterInvalidConfigError on failure; manually building JSON Schema dicts and calling
      jsonschema.validate() with ValidationError is absent from the Dagster corpus — none of
      Field, Shape, ConfigSchema, or @config_mapping appear"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/job_definition.py
    host_inject_at_line: 800

  - id: dagster_py_validation_2
    language: python
    file: breaks/py/paradigm_break_pydantic_model.py
    category: validation
    class: semantic_convention
    hunk_start_line: 14
    hunk_end_line: 68
    rationale: "Dagster's dagster.Config (a Pydantic superclass) integrates with the event log and
      MetadataValue; declaring standalone Pydantic v2 BaseModel classes with field_validator /
      model_validator and calling model_validate() / model_dump() bypasses dagster.Config,
      MetadataValue, AssetMaterialization.metadata, and DagsterType — all absent from the corpus"
    difficulty: hard
    host_file: python_modules/dagster/dagster/_core/definitions/op_definition.py
    host_inject_at_line: 700

  - id: dagster_py_exception_handling_1
    language: python
    file: breaks/py/paradigm_break_broad_except.py
    category: exception_handling
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 80
    rationale: "Dagster handles transient failures via RetryPolicy(max_retries=N, backoff=Backoff.EXPONENTIAL)
      attached to op definitions; broad except Exception blocks with time.sleep() retry loops and
      silent empty returns on extract failure replace RetryPolicy, Backoff, Jitter, and @failure_hook
      — none of which appear in the Dagster corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/policy.py
    host_inject_at_line: 85

  - id: dagster_py_exception_handling_2
    language: python
    file: breaks/py/paradigm_break_atexit_handler.py
    category: exception_handling
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 70
    rationale: "Dagster uses @failure_hook / @success_hook decorators receiving HookContext for
      structured lifecycle responses; atexit.register() callbacks and signal.signal(SIGTERM, ...)
      handlers that mutate a module-level _pipeline_state dict are absent from the Dagster corpus —
      @failure_hook, HookContext, op_failure_event, and RetryPolicy do not appear"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/sensor_definition.py
    host_inject_at_line: 500

  - id: dagster_py_asset_definition_3
    language: python
    file: breaks/py/paradigm_break_raw_boto3_io.py
    category: asset_definition
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 67
    rationale: "Dagster manages S3 persistence via IOManager / UPathIOManager subclasses implementing
      handle_output(context, obj) and load_input(context) with S3Resource; constructing a boto3 client
      directly and calling put_object / get_object with pickle serialisation bypasses IOManager,
      OutputContext, InputContext, and UPathIOManager — none of which appear in the Dagster corpus"
    difficulty: medium
    host_file: python_modules/libraries/dagster-aws/dagster_aws/s3/io_manager.py
    host_inject_at_line: 100

  # ---------------------------------------------------------------------
  # TypeScript — Dagit (UI) vs other-framework paradigms
  # ---------------------------------------------------------------------
  - id: dagster_ts_framework_swap_1
    language: typescript
    file: breaks/ts/break_framework_swap_1.tsx
    category: framework_swap
    class: foreign_api
    hunk_start_line: 5
    hunk_end_line: 51
    rationale: >-
      Redux connect() HOC wrapping a class component with componentDidMount lifecycle method,
      mapStateToProps, and mapDispatchToProps — Dagit uses function components with useRecoilState
      for shared state and custom domain hooks. The react-redux connect pattern and class components
      are entirely absent from the codebase.
    difficulty: medium
    host_file: js_modules/ui-core/src/assets/AssetEvents.tsx
    host_inject_at_line: 30

  - id: dagster_ts_framework_swap_2
    language: typescript
    file: breaks/ts/break_framework_swap_2.tsx
    category: framework_swap
    class: foreign_api
    hunk_start_line: 5
    hunk_end_line: 44
    rationale: >-
      jQuery $.ajax + imperative DOM mutation ($container.empty(), .append(), .on()) inside
      what should be a React component. Dagit renders all UI through React function components
      using Box/Icon/Colors from @dagster-io/ui-components; direct DOM manipulation via jQuery
      selectors has no place in the codebase.
    difficulty: easy
    host_file: js_modules/ui-core/src/assets/AssetActionMenu.tsx
    host_inject_at_line: 25

  - id: dagster_ts_framework_swap_3
    language: typescript
    file: breaks/ts/break_framework_swap_3.tsx
    category: framework_swap
    class: foreign_api
    hunk_start_line: 5
    hunk_end_line: 48
    rationale: >-
      Vue 3 Composition API — defineComponent, ref, computed, template string with v-if and
      @click directives — transplanted into a React/TypeScript file. Dagit is entirely React
      with JSX; Vue-specific APIs (defineComponent, setup(), vue template) are structurally
      incompatible with the codebase.
    difficulty: easy
    host_file: js_modules/ui-core/src/assets/AssetHealthSummary.tsx
    host_inject_at_line: 50

  - id: dagster_ts_state_management_1
    language: typescript
    file: breaks/ts/break_state_management_1.tsx
    category: state_management
    class: foreign_api
    hunk_start_line: 5
    hunk_end_line: 60
    rationale: >-
      MobX makeAutoObservable class store + observer() HOC from mobx-react-lite instead of
      Dagit's Recoil atoms (useRecoilState/useRecoilValue). Dagit wraps the whole app in
      RecoilRoot; MobX and its observable/observer pattern are not present anywhere in the
      ui-core or app-oss packages.
    difficulty: hard
    host_file: js_modules/ui-core/src/overview/OverviewJobsTable.tsx
    host_inject_at_line: 20

  - id: dagster_ts_state_management_2
    language: typescript
    file: breaks/ts/break_state_management_2.tsx
    category: state_management
    class: foreign_api
    hunk_start_line: 5
    hunk_end_line: 48
    rationale: >-
      Redux Toolkit createSlice + configureStore + useDispatch/useSelector for local UI filter
      state, whereas Dagit uses Recoil atoms and useQueryPersistedState (URL-synced hook) for
      shared and persistent UI state. @reduxjs/toolkit and react-redux are absent from Dagit's
      package.json.
    difficulty: hard
    host_file: js_modules/ui-core/src/app/AppProvider.tsx
    host_inject_at_line: 40

  - id: dagster_ts_data_fetching_1
    language: typescript
    file: breaks/ts/break_data_fetching_1.tsx
    category: data_fetching
    class: semantic_convention
    hunk_start_line: 6
    hunk_end_line: 65
    rationale: >-
      Raw fetch() + useEffect + manual {data, loading, error} state triple for REST data
      instead of Dagit's Apollo useQuery wrapping GraphQL operations. Dagit has no REST
      fetch-with-useState pattern in its UI layer; every data request goes through Apollo
      Client and a generated GraphQL query.
    difficulty: medium
    host_file: js_modules/ui-core/src/live-data-provider/LiveDataProvider.tsx
    host_inject_at_line: 35

  - id: dagster_ts_data_fetching_2
    language: typescript
    file: breaks/ts/break_data_fetching_2.tsx
    category: data_fetching
    class: semantic_convention
    hunk_start_line: 6
    hunk_end_line: 49
    rationale: >-
      Axios + setInterval manual polling loop for live asset materialization status instead of
      Dagit's Apollo Client + LiveDataProvider, which manages refresh cadence via
      LiveDataPollRateContext (default 2000 ms). The axios package and raw setInterval timers
      for UI polling are absent from Dagit's codebase.
    difficulty: hard
    host_file: js_modules/ui-core/src/assets/AssetEventDetail.tsx
    host_inject_at_line: 22

  - id: dagster_ts_routing_1
    language: typescript
    file: breaks/ts/break_routing_1.tsx
    category: routing
    class: foreign_api
    hunk_start_line: 6
    hunk_end_line: 50
    rationale: >-
      React Router v5 Switch + Route with component= prop for page routing instead of Dagit's
      Next.js pages directory. Dagit is a Next.js app; page-level routing is handled by the
      file-system pages/ convention and useRouter() from next/router. React Router's Switch and
      Route component= pattern conflict with the Next.js model.
    difficulty: medium
    host_file: js_modules/ui-core/src/overview/OverviewActivityRoot.tsx
    host_inject_at_line: 20

  - id: dagster_ts_routing_2
    language: typescript
    file: breaks/ts/break_routing_2.tsx
    category: routing
    class: foreign_api
    hunk_start_line: 6
    hunk_end_line: 44
    rationale: >-
      React Router v5 withRouter HOC injecting history.push into a class component for
      programmatic navigation. Dagit navigates programmatically using useRouter() from
      next/router inside function components; withRouter and history.push are React Router
      patterns with no analog in the Next.js-based codebase.
    difficulty: medium
    host_file: js_modules/ui-core/src/nav/PipelineNav.tsx
    host_inject_at_line: 22

  - id: dagster_ts_styling_1
    language: typescript
    file: breaks/ts/break_styling_1.tsx
    category: styling
    class: foreign_api
    hunk_start_line: 6
    hunk_end_line: 48
    rationale: >-
      CSS Modules import (*.module.css) + classnames utility for conditional class names instead
      of Dagit's @dagster-io/ui-components design-system (Box flex props, Colors token functions,
      Icon primitives). CSS module files and the classnames package are not used anywhere in the
      ui-core or app-oss packages.
    difficulty: hard
    host_file: js_modules/ui-core/src/hooks/useQueryPersistedState.tsx
    host_inject_at_line: 50

  - id: dagster_ts_styling_2
    language: typescript
    file: breaks/ts/break_styling_2.tsx
    category: styling
    class: foreign_api
    hunk_start_line: 6
    hunk_end_line: 50
    rationale: >-
      styled-jsx scoped <style jsx>{``}</style> blocks for component-level CSS-in-JSX instead
      of Dagit's @dagster-io/ui-components Box/Colors system. Though styled-jsx ships with
      Next.js, Dagit applies no scoped style blocks; visual styling goes exclusively through the
      @dagster-io/ui-components token system.
    difficulty: medium
    host_file: js_modules/ui-core/src/instigation/InstigationTick.tsx
    host_inject_at_line: 14

  - id: dagster_ts_framework_swap_4
    language: typescript
    file: breaks/ts/break_type_paradigm_1.ts
    category: framework_swap
    class: naming_shape_break
    hunk_start_line: 6
    hunk_end_line: 52
    rationale: >-
      JSDoc-annotated JavaScript posing as TypeScript: `any` on every parameter and return type,
      `var` declarations, and bare `function()` expressions. Dagit enforces strict TypeScript
      (no-any, no implicit any, all props/state fully typed with explicit interfaces). This
      pattern would fail oxlint no-explicit-any and tsc strict checks throughout the codebase.
    difficulty: easy
    host_file: js_modules/ui-core/src/assets/AssetLink.tsx
    host_inject_at_line: 13

  # =====================================================================
  # RUBRIC v2 foreign-dependency breaks (#92 graded recall sample)
  # Every dependency below is verified 0-usage at the pinned SHA across the
  # whole monorepo (source + examples/, which argot trains on) AND absent from
  # every setup.py / pyproject.toml / package.json. Difficulty-graded per the
  # graded-brief: easy fires via the import stage; medium stresses callee /
  # receiver resolution but still carries a foreign-root import; hard masks the
  # foreignness (attested root namespace, dynamic import, or a leaf name that
  # collides with an attested method) and may honestly MISS.
  # ---------------------------------------------------------------------
  # Python — foreign packages (dagster is a monorepo: dask/celery/snowflake/
  # gcp/aws/azure/duckdb/polars/pydantic/airflow/prefect are all ATTESTED and
  # deliberately avoided).
  # ---------------------------------------------------------------------
  - id: dagster_py_foreign_import_temporalio
    language: python
    file: breaks/py/foreign_import_temporalio.py
    category: foreign_import
    hunk_start_line: 11
    hunk_end_line: 47
    rationale: "Temporal Python SDK (temporalio) @workflow.defn / @activity.defn durable-workflow
      pipeline replacing Dagster's @op/@job. Verified 0-usage: `import/from temporalio` = 0 across
      all *.py at SHA 549b894e (whole monorepo incl. examples/), and `temporalio` absent from every
      setup.py/pyproject.toml. Import stage roots to the foreign `temporalio` module."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/job_definition.py
    host_inject_at_line: 96

  - id: dagster_py_foreign_import_loguru
    language: python
    file: breaks/py/foreign_import_loguru.py
    category: foreign_import
    hunk_start_line: 9
    hunk_end_line: 21
    rationale: "loguru process-global logger (logger.add/remove/bind) replacing Dagster's
      context.log / get_dagster_logger(). Verified 0-usage: no `import loguru` / `from loguru`
      anywhere at SHA 549b894e; the sole `loguru` token is the string \"loguru==0.6.0\" in a Modal
      image-requirements list (examples/), not an import, so the module is unattested. Dagster's own
      logging is structlog-based (attested) — loguru is the foreign dep. Import stage fires on `loguru`."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/op_definition.py
    host_inject_at_line: 59

  - id: dagster_py_foreign_concurrency_ray
    language: python
    file: breaks/py/foreign_concurrency_ray.py
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 9
    hunk_end_line: 23
    rationale: "Ray distributed runtime (@ray.remote / ray.init / ray.get) fanning steps out instead
      of Dagster's own multiprocess_executor / StepDelegatingExecutor. Verified 0-usage: `import ray`
      / `from ray` = 0 across all *.py at SHA 549b894e, `ray` absent from every dependency manifest
      (no dagster-ray integration exists). Import stage roots to the foreign `ray` module."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/executor/multiprocess.py
    host_inject_at_line: 53

  - id: dagster_py_foreign_concurrency_trio
    language: python
    file: breaks/py/foreign_concurrency_trio.py
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 9
    hunk_end_line: 26
    rationale: "trio structured-concurrency runtime (trio.open_nursery / trio.run / nursery.start_soon)
      driving concurrent sensor evaluation instead of Dagster's daemon loop. Verified 0-usage:
      `import trio` / `from trio` = 0 across all *.py at SHA 549b894e and absent from every dependency
      manifest (dagster's async is asyncio/anyio, not trio). Import stage fires on the foreign `trio`."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/sensor_definition.py
    host_inject_at_line: 77

  - id: dagster_py_foreign_api_pymongo
    language: python
    file: breaks/py/foreign_api_pymongo.py
    category: foreign_api
    hunk_start_line: 9
    hunk_end_line: 36
    rationale: "MongoDB run persistence via pymongo MongoClient (client[db][coll].insert_one /
      find_one / update_one, reached through a receiver variable) instead of Dagster's SqlRunStorage
      over SQLAlchemy. Verified 0-usage: `import/from pymongo` = 0 across all *.py at SHA 549b894e,
      `pymongo` absent from every dependency manifest. Foreign-root import (pymongo) carries the tell;
      the API is exercised through a `collection` receiver var."
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/storage/runs/sql_run_storage.py
    host_inject_at_line: 85

  - id: dagster_py_foreign_concurrency_gevent
    language: python
    file: breaks/py/foreign_concurrency_gevent.py
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 9
    hunk_end_line: 22
    rationale: "gevent greenlet Pool (gevent.pool.Pool / pool.spawn / gevent.sleep) for concurrent
      step dispatch instead of Dagster's StepDelegatingExecutor. Verified 0-usage: no `import gevent`
      / `from gevent` at SHA 549b894e; the only `gevent` token is a string entry in
      docs/sphinx/conf.py (autodoc mock list), not an import, so the module is unattested. Submodule
      import `from gevent.pool import Pool` roots to the foreign `gevent`."
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/executor/step_delegating/step_delegating_executor.py
    host_inject_at_line: 30

  - id: dagster_py_foreign_import_backoff
    language: python
    file: breaks/py/foreign_import_backoff.py
    category: foreign_import
    hunk_start_line: 9
    hunk_end_line: 26
    rationale: "Third-party `backoff` package (@backoff.on_exception(backoff.expo/fibo, ...,
      jitter=backoff.full_jitter)) replacing Dagster's declarative RetryPolicy(backoff=Backoff.EXPONENTIAL,
      jitter=Jitter...). Verified 0-usage: no `import backoff` / `from backoff` anywhere at SHA 549b894e
      and `backoff` absent from every dependency manifest. The ~95 `backoff` string hits are Dagster's
      OWN `Backoff` enum / `backoff_factor` kwargs (an attested token but NOT this module) — a genuine
      masked case where BPE is unsurprised yet the module import is foreign. Import stage fires on `backoff`."
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/policy.py
    host_inject_at_line: 10

  - id: dagster_py_foreign_api_firestore
    language: python
    file: breaks/py/foreign_api_firestore.py
    category: foreign_api
    hunk_start_line: 12
    hunk_end_line: 26
    rationale: "HARD (masked): Google Cloud Firestore IO (google.cloud.firestore.Client().collection().
      document().set()) reached through the ATTESTED `google` root. Verified: `google` root IS attested
      (google.cloud.storage, google.api_core, google.protobuf, google.cloud.dataproc_v1 in dagster-gcp),
      but `google.cloud.firestore` is 0-usage (the only `firestore` token is a doc-URL comment in
      dagster-airbyte, not an import). Import stage keys on the `google` root (attested → silent);
      the client is reached via a receiver var, so the leaf methods carry no foreign namespace. Genuine
      likely-miss — reported honestly."
    difficulty: hard
    host_file: python_modules/libraries/dagster-gcp/dagster_gcp/gcs/io_manager.py
    host_inject_at_line: 28

  - id: dagster_py_foreign_api_cosmos
    language: python
    file: breaks/py/foreign_api_cosmos.py
    category: foreign_api
    hunk_start_line: 11
    hunk_end_line: 31
    rationale: "HARD (masked): Azure Cosmos DB (azure.cosmos.CosmosClient().get_database_client().
      get_container_client().upsert_item()) reached through the ATTESTED `azure` root. Verified: `azure`
      root IS attested (azure.storage.blob/filedatalake, azure.identity, azure.ai.ml in dagster-azure),
      but `azure.cosmos` = 0 usage across all *.py at SHA 549b894e. Import stage keys on the `azure`
      root (attested → silent); the client is reached via receiver vars. Genuine likely-miss — reported
      honestly."
    difficulty: hard
    host_file: python_modules/libraries/dagster-azure/dagster_azure/adls2/resources.py
    host_inject_at_line: 27

  # ---------------------------------------------------------------------
  # TypeScript — Dagit (ui-core) foreign packages. Attested and avoided:
  # @apollo/client, recoil, styled-components, @dagster-io/ui-components, react-*,
  # lodash, dayjs, date-fns, graphql, chart.js, @visx/*, @koale/useworker.
  # ---------------------------------------------------------------------
  - id: dagster_ts_foreign_import_zustand
    language: typescript
    file: breaks/ts/foreign_import_zustand.tsx
    category: foreign_import
    hunk_start_line: 5
    hunk_end_line: 21
    rationale: "zustand create() global store replacing Dagit's Recoil atoms (useRecoilState under
      RecoilRoot) and useQueryPersistedState. Verified 0-usage: `zustand` = 0 across all *.ts/*.tsx in
      js_modules at SHA 549b894e and absent from ui-core package.json. Import stage keys on the full
      `zustand` specifier (foreign)."
    difficulty: easy
    host_file: js_modules/ui-core/src/runs/RunTimeline.tsx
    host_inject_at_line: 46

  - id: dagster_ts_foreign_concurrency_rxjs
    language: typescript
    file: breaks/ts/foreign_concurrency_rxjs.tsx
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 5
    hunk_end_line: 24
    rationale: "RxJS reactive-streams runtime (Subject / interval / pipe(switchMap).subscribe) for log
      polling instead of Dagit's Apollo pollInterval + LiveDataPollRateContext. Verified 0-usage: `rxjs`
      and `rxjs/operators` = 0 across all *.ts/*.tsx in js_modules and absent from ui-core package.json.
      Import stage fires on the foreign `rxjs` specifier(s)."
    difficulty: easy
    host_file: js_modules/ui-core/src/asset-graph/AssetRunLogObserver.tsx
    host_inject_at_line: 10

  - id: dagster_ts_foreign_api_zod
    language: typescript
    file: breaks/ts/foreign_api_zod.tsx
    category: foreign_api
    hunk_start_line: 4
    hunk_end_line: 22
    rationale: "zod runtime schema validation (z.object(...).parse()/.safeParse()) for launch config
      instead of Dagit's server-side GraphQL-typed config + the `yaml` package. Verified 0-usage: `zod`
      = 0 across all *.ts/*.tsx in js_modules and absent from ui-core package.json. Import stage fires
      on the foreign `zod` specifier; the z.* API is the foreign-callee surface."
    difficulty: easy
    host_file: js_modules/ui-core/src/launchpad/LaunchpadRoot.tsx
    host_inject_at_line: 25

  - id: dagster_ts_foreign_import_framer_motion
    language: typescript
    file: breaks/ts/foreign_import_framer_motion.tsx
    category: foreign_import
    hunk_start_line: 5
    hunk_end_line: 21
    rationale: "framer-motion animation runtime (<motion.div>, useAnimation controls) instead of Dagit's
      styled-components keyframes/transition props. Verified 0-usage: `framer-motion` = 0 across all
      *.ts/*.tsx in js_modules and absent from ui-core package.json. Import stage fires on the foreign
      `framer-motion` specifier; the `controls` receiver var drives the API (medium)."
    difficulty: medium
    host_file: js_modules/ui-core/src/asset-graph/AssetNode.tsx
    host_inject_at_line: 38

  - id: dagster_ts_foreign_import_react_hook_form
    language: typescript
    file: breaks/ts/foreign_import_react_hook_form.tsx
    category: foreign_import
    hunk_start_line: 4
    hunk_end_line: 25
    rationale: "react-hook-form form-state library (useForm / register / handleSubmit) instead of
      Dagit's @dagster-io/ui-components controls wired to useState / Recoil. Verified 0-usage:
      `react-hook-form` = 0 across all *.ts/*.tsx in js_modules and absent from ui-core package.json.
      Import stage fires on the foreign specifier; the register/handleSubmit API comes off a
      destructured useForm() result (medium)."
    difficulty: medium
    host_file: js_modules/ui-core/src/sensors/SensorRoot.tsx
    host_inject_at_line: 30

  - id: dagster_ts_foreign_concurrency_comlink
    language: typescript
    file: breaks/ts/foreign_concurrency_comlink.ts
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 4
    hunk_end_line: 21
    rationale: "Comlink web-worker RPC bridge (Comlink.wrap<T>(worker) / proxy) for off-thread graph
      layout instead of Dagit's worker-loader + @koale/useworker pattern. Verified 0-usage: `comlink`
      = 0 across all *.ts/*.tsx in js_modules and absent from ui-core package.json. Import stage fires
      on the foreign `comlink` specifier; the wrapped `api` proxy is reached via a receiver (medium)."
    difficulty: medium
    host_file: js_modules/ui-core/src/asset-graph/ComputeGraphData.ts
    host_inject_at_line: 10

  - id: dagster_ts_foreign_api_xlsx_dynamic
    language: typescript
    file: breaks/ts/foreign_api_xlsx_dynamic.tsx
    category: foreign_api
    hunk_start_line: 6
    hunk_end_line: 15
    rationale: "HARD (masked): SheetJS `xlsx` (XLSX.utils.json_to_sheet / writeFile) run-table export
      loaded via a dynamic `await import('xlsx')` EXPRESSION — deliberately not a static import
      statement, which the import-graph adapter (`import ... from` only) never observes. Verified
      0-usage: `xlsx` = 0 across all *.ts/*.tsx in js_modules and absent from ui-core package.json. The
      XLSX handle is a local const, so its leaf calls carry no foreign namespace. Genuine likely-miss
      (import-graph blind spot) — reported honestly."
    difficulty: hard
    host_file: js_modules/ui-core/src/runs/RunsFeedTable.tsx
    host_inject_at_line: 34

  - id: dagster_ts_foreign_api_react_query
    language: typescript
    file: breaks/ts/foreign_api_react_query.tsx
    category: foreign_api
    hunk_start_line: 5
    hunk_end_line: 28
    rationale: "HARD (masked): @tanstack/react-query useQuery({queryKey, queryFn}) + QueryClient whose
      leaf hook name `useQuery` COLLIDES with Dagit's attested Apollo useQuery (from @apollo/client).
      Verified 0-usage: `@tanstack/react-query` = 0 across all *.ts/*.tsx in js_modules and absent from
      ui-core package.json (only @tanstack/react-virtual is attested — a different specifier). The
      colliding leaf name is the intended mask; the TS import stage keys on the full package specifier,
      so this likely CATCHES despite the collision — reported honestly."
    difficulty: hard
    host_file: js_modules/ui-core/src/runs/RunRoot.tsx
    host_inject_at_line: 38
