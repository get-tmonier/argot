corpus: dagster
language: multi
categories:
  # Python categories
  - framework_swap
  - asset_definition
  - scheduling
  - validation
  - exception_handling
  # TypeScript categories
  - state_management
  - data_fetching
  - routing
  - styling
  # RUBRIC v2 gated novel-pattern classes (foreign-dependency breaks)
  - foreign_import
  - foreign_api
  - foreign_concurrency
injection_hosts:
  - pr: 0
    sha: 549b894e4ba03d5836459d58932105b77fafe242
fixtures:
  # ---------------------------------------------------------------------
  # Python — Dagster vs other-framework paradigms
  # ---------------------------------------------------------------------
  - id: dagster_py_framework_swap_2
    language: python
    file: breaks/py/paradigm_break_prefect_task.py
    category: framework_swap
    class: foreign_api
    hunk_start_line: 14
    hunk_end_line: 53
    rationale: "Dagster uses @op / @asset with RetryPolicy objects; Prefect's @task(retries=3,
      retry_delay_seconds=exponential_backoff(...)), @flow, PrefectFuture, and task_runner
      parameters are entirely absent from the Dagster corpus"
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/op_definition.py
    host_inject_at_line: 350

  - id: dagster_py_framework_swap_3
    language: python
    file: breaks/py/paradigm_break_luigi_pipeline.py
    category: framework_swap
    class: foreign_api
    hunk_start_line: 15
    hunk_end_line: 79
    rationale: "Dagster expresses asset dependencies via parameter names or deps=[]; Luigi's class-based
      Task subclasses with requires() / output() / run() methods, luigi.LocalTarget, luigi.DateParameter,
      self.input().open(), and luigi.build() are absent from the Dagster corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/job_definition.py
    host_inject_at_line: 600

  - id: dagster_py_asset_definition_1
    language: python
    file: breaks/py/paradigm_break_procedural_etl.py
    category: asset_definition
    class: semantic_convention
    hunk_start_line: 14
    hunk_end_line: 72
    rationale: "Dagster models data as @asset-decorated functions with return values persisted by an
      IOManager; this file uses plain module-level functions that mutate a shared _state dict and call
      each step explicitly in run_pipeline() — @asset, AssetIn, IOManager, Definitions, and
      MaterializeResult are absent from the corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/definitions_class.py
    host_inject_at_line: 500

  - id: dagster_py_asset_definition_2
    language: python
    file: breaks/py/paradigm_break_class_pipeline.py
    category: asset_definition
    class: semantic_convention
    hunk_start_line: 13
    hunk_end_line: 76
    rationale: "Dagster models data with @asset-decorated pure functions wired by the asset graph;
      a Pipeline class that stores intermediate data as self.raw_rows / self.clean_rows and calls
      connect() / extract() / transform() / load() in sequence replaces the entire @asset /
      Definitions vocabulary absent from the corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/definitions_class.py
    host_inject_at_line: 800

  - id: dagster_py_scheduling_1
    language: python
    file: breaks/py/paradigm_break_apscheduler.py
    category: scheduling
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 76
    rationale: "Dagster declares schedules as ScheduleDefinition objects registered in Definitions()
      with cron_schedule, execution_timezone, and DefaultScheduleStatus managed by the Dagster daemon;
      APScheduler's BlockingScheduler, scheduler.add_job(), CronTrigger, and replace_existing=True
      are absent from the Dagster corpus"
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/schedule_definition.py
    host_inject_at_line: 650

  - id: dagster_py_scheduling_2
    language: python
    file: breaks/py/paradigm_break_cron_schedule.py
    category: scheduling
    class: semantic_convention
    hunk_start_line: 14
    hunk_end_line: 75
    rationale: "Dagster uses ScheduleDefinition with execution_fn yielding RunRequest objects evaluated
      by the Dagster daemon; croniter for next-fire computation, threading.Event for sleep, and
      a manual _schedule_loop() with try/except are absent from the Dagster corpus — no RunRequest,
      cron_schedule field, or job_name appear"
    difficulty: hard
    host_file: python_modules/dagster/dagster/_core/definitions/schedule_definition.py
    host_inject_at_line: 950

  - id: dagster_py_validation_1
    language: python
    file: breaks/py/paradigm_break_jsonschema_validation.py
    category: validation
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 83
    rationale: "Dagster validates run config through dagster.Field / dagster.Shape / dagster.Array
      with DagsterInvalidConfigError on failure; manually building JSON Schema dicts and calling
      jsonschema.validate() with ValidationError is absent from the Dagster corpus — none of
      Field, Shape, ConfigSchema, or @config_mapping appear"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/job_definition.py
    host_inject_at_line: 800

  - id: dagster_py_validation_2
    language: python
    file: breaks/py/paradigm_break_pydantic_model.py
    category: validation
    class: semantic_convention
    hunk_start_line: 14
    hunk_end_line: 68
    rationale: "Dagster's dagster.Config (a Pydantic superclass) integrates with the event log and
      MetadataValue; declaring standalone Pydantic v2 BaseModel classes with field_validator /
      model_validator and calling model_validate() / model_dump() bypasses dagster.Config,
      MetadataValue, AssetMaterialization.metadata, and DagsterType — all absent from the corpus"
    difficulty: hard
    host_file: python_modules/dagster/dagster/_core/definitions/op_definition.py
    host_inject_at_line: 700

  - id: dagster_py_exception_handling_1
    language: python
    file: breaks/py/paradigm_break_broad_except.py
    category: exception_handling
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 80
    rationale: "Dagster handles transient failures via RetryPolicy(max_retries=N, backoff=Backoff.EXPONENTIAL)
      attached to op definitions; broad except Exception blocks with time.sleep() retry loops and
      silent empty returns on extract failure replace RetryPolicy, Backoff, Jitter, and @failure_hook
      — none of which appear in the Dagster corpus"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/policy.py
    host_inject_at_line: 85

  - id: dagster_py_exception_handling_2
    language: python
    file: breaks/py/paradigm_break_atexit_handler.py
    category: exception_handling
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 70
    rationale: "Dagster uses @failure_hook / @success_hook decorators receiving HookContext for
      structured lifecycle responses; atexit.register() callbacks and signal.signal(SIGTERM, ...)
      handlers that mutate a module-level _pipeline_state dict are absent from the Dagster corpus —
      @failure_hook, HookContext, op_failure_event, and RetryPolicy do not appear"
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/sensor_definition.py
    host_inject_at_line: 500

  - id: dagster_py_asset_definition_3
    language: python
    file: breaks/py/paradigm_break_raw_boto3_io.py
    category: asset_definition
    class: semantic_convention
    hunk_start_line: 15
    hunk_end_line: 67
    rationale: "Dagster manages S3 persistence via IOManager / UPathIOManager subclasses implementing
      handle_output(context, obj) and load_input(context) with S3Resource; constructing a boto3 client
      directly and calling put_object / get_object with pickle serialisation bypasses IOManager,
      OutputContext, InputContext, and UPathIOManager — none of which appear in the Dagster corpus"
    difficulty: medium
    host_file: python_modules/libraries/dagster-aws/dagster_aws/s3/io_manager.py
    host_inject_at_line: 100

  # ---------------------------------------------------------------------
  # TypeScript — Dagit (UI) vs other-framework paradigms
  # ---------------------------------------------------------------------
  - id: dagster_py_foreign_import_temporalio
    language: python
    file: breaks/py/foreign_import_temporalio.py
    category: foreign_import
    hunk_start_line: 11
    hunk_end_line: 47
    rationale: "Temporal Python SDK (temporalio) @workflow.defn / @activity.defn durable-workflow
      pipeline replacing Dagster's @op/@job. Verified 0-usage: `import/from temporalio` = 0 across
      all *.py at SHA 549b894e (whole monorepo incl. examples/), and `temporalio` absent from every
      setup.py/pyproject.toml. Import stage roots to the foreign `temporalio` module."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/job_definition.py
    host_inject_at_line: 96

  - id: dagster_py_foreign_import_loguru
    language: python
    file: breaks/py/foreign_import_loguru.py
    category: foreign_import
    hunk_start_line: 9
    hunk_end_line: 21
    rationale: "loguru process-global logger (logger.add/remove/bind) replacing Dagster's
      context.log / get_dagster_logger(). Verified 0-usage: no `import loguru` / `from loguru`
      anywhere at SHA 549b894e; the sole `loguru` token is the string \"loguru==0.6.0\" in a Modal
      image-requirements list (examples/), not an import, so the module is unattested. Dagster's own
      logging is structlog-based (attested) — loguru is the foreign dep. Import stage fires on `loguru`."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/op_definition.py
    host_inject_at_line: 59

  - id: dagster_py_foreign_concurrency_ray
    language: python
    file: breaks/py/foreign_concurrency_ray.py
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 9
    hunk_end_line: 23
    rationale: "Ray distributed runtime (@ray.remote / ray.init / ray.get) fanning steps out instead
      of Dagster's own multiprocess_executor / StepDelegatingExecutor. Verified 0-usage: `import ray`
      / `from ray` = 0 across all *.py at SHA 549b894e, `ray` absent from every dependency manifest
      (no dagster-ray integration exists). Import stage roots to the foreign `ray` module."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/executor/multiprocess.py
    host_inject_at_line: 53

  - id: dagster_py_foreign_concurrency_trio
    language: python
    file: breaks/py/foreign_concurrency_trio.py
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 9
    hunk_end_line: 26
    rationale: "trio structured-concurrency runtime (trio.open_nursery / trio.run / nursery.start_soon)
      driving concurrent sensor evaluation instead of Dagster's daemon loop. Verified 0-usage:
      `import trio` / `from trio` = 0 across all *.py at SHA 549b894e and absent from every dependency
      manifest (dagster's async is asyncio/anyio, not trio). Import stage fires on the foreign `trio`."
    difficulty: easy
    host_file: python_modules/dagster/dagster/_core/definitions/sensor_definition.py
    host_inject_at_line: 77

  - id: dagster_py_foreign_api_pymongo
    language: python
    file: breaks/py/foreign_api_pymongo.py
    category: foreign_api
    hunk_start_line: 9
    hunk_end_line: 36
    rationale: "MongoDB run persistence via pymongo MongoClient (client[db][coll].insert_one /
      find_one / update_one, reached through a receiver variable) instead of Dagster's SqlRunStorage
      over SQLAlchemy. Verified 0-usage: `import/from pymongo` = 0 across all *.py at SHA 549b894e,
      `pymongo` absent from every dependency manifest. Foreign-root import (pymongo) carries the tell;
      the API is exercised through a `collection` receiver var."
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/storage/runs/sql_run_storage.py
    host_inject_at_line: 85

  - id: dagster_py_foreign_concurrency_gevent
    language: python
    file: breaks/py/foreign_concurrency_gevent.py
    category: foreign_concurrency
    class: foreign_import
    hunk_start_line: 9
    hunk_end_line: 22
    rationale: "gevent greenlet Pool (gevent.pool.Pool / pool.spawn / gevent.sleep) for concurrent
      step dispatch instead of Dagster's StepDelegatingExecutor. Verified 0-usage: no `import gevent`
      / `from gevent` at SHA 549b894e; the only `gevent` token is a string entry in
      docs/sphinx/conf.py (autodoc mock list), not an import, so the module is unattested. Submodule
      import `from gevent.pool import Pool` roots to the foreign `gevent`."
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/executor/step_delegating/step_delegating_executor.py
    host_inject_at_line: 30

  - id: dagster_py_foreign_import_backoff
    language: python
    file: breaks/py/foreign_import_backoff.py
    category: foreign_import
    hunk_start_line: 9
    hunk_end_line: 26
    rationale: "Third-party `backoff` package (@backoff.on_exception(backoff.expo/fibo, ...,
      jitter=backoff.full_jitter)) replacing Dagster's declarative RetryPolicy(backoff=Backoff.EXPONENTIAL,
      jitter=Jitter...). Verified 0-usage: no `import backoff` / `from backoff` anywhere at SHA 549b894e
      and `backoff` absent from every dependency manifest. The ~95 `backoff` string hits are Dagster's
      OWN `Backoff` enum / `backoff_factor` kwargs (an attested token but NOT this module) — a genuine
      masked case where BPE is unsurprised yet the module import is foreign. Import stage fires on `backoff`."
    difficulty: medium
    host_file: python_modules/dagster/dagster/_core/definitions/policy.py
    host_inject_at_line: 10

  - id: dagster_py_foreign_api_firestore
    language: python
    file: breaks/py/foreign_api_firestore.py
    category: foreign_api
    hunk_start_line: 12
    hunk_end_line: 26
    rationale: "HARD (masked): Google Cloud Firestore IO (google.cloud.firestore.Client().collection().
      document().set()) reached through the ATTESTED `google` root. Verified: `google` root IS attested
      (google.cloud.storage, google.api_core, google.protobuf, google.cloud.dataproc_v1 in dagster-gcp),
      but `google.cloud.firestore` is 0-usage (the only `firestore` token is a doc-URL comment in
      dagster-airbyte, not an import). Import stage keys on the `google` root (attested → silent);
      the client is reached via a receiver var, so the leaf methods carry no foreign namespace. Genuine
      likely-miss — reported honestly."
    difficulty: hard
    host_file: python_modules/libraries/dagster-gcp/dagster_gcp/gcs/io_manager.py
    host_inject_at_line: 28

  - id: dagster_py_foreign_api_cosmos
    language: python
    file: breaks/py/foreign_api_cosmos.py
    category: foreign_api
    hunk_start_line: 11
    hunk_end_line: 31
    rationale: "HARD (masked): Azure Cosmos DB (azure.cosmos.CosmosClient().get_database_client().
      get_container_client().upsert_item()) reached through the ATTESTED `azure` root. Verified: `azure`
      root IS attested (azure.storage.blob/filedatalake, azure.identity, azure.ai.ml in dagster-azure),
      but `azure.cosmos` = 0 usage across all *.py at SHA 549b894e. Import stage keys on the `azure`
      root (attested → silent); the client is reached via receiver vars. Genuine likely-miss — reported
      honestly."
    difficulty: hard
    host_file: python_modules/libraries/dagster-azure/dagster_azure/adls2/resources.py
    host_inject_at_line: 27

  # ---------------------------------------------------------------------
  # TypeScript — Dagit (ui-core) foreign packages. Attested and avoided:
  # @apollo/client, recoil, styled-components, @dagster-io/ui-components, react-*,
  # lodash, dayjs, date-fns, graphql, chart.js, @visx/*, @koale/useworker.
  # ---------------------------------------------------------------------