corpus: bat
language: rust
categories:
- foreign_import
- foreign_api
- foreign_concurrency
injection_hosts:
- pr: 0
  sha: 78951393e29bfd2f2a45f4326b9d2bb5e737dd2a
fixtures:
- id: foreign_reqwest_import_1
  file: breaks/break_foreign_reqwest_import.rs
  category: foreign_import
  hunk_start_line: 16
  hunk_end_line: 27
  rationale: 'use reqwest::blocking::Client + reqwest::blocking::Client::builder()
    pulling a theme set over HTTP, import inside hunk. Verified foreign at SHA
    78951393e29b: `reqwest` = 0 grep hits across *.rs and absent from Cargo.toml;
    bat loads every theme/syntax asset from an embedded include_bytes! blob or
    the local cache dir (assets.rs get_serialized_integrated_syntaxset /
    asset_from_cache), never over the network.'
  difficulty: easy
  host_file: src/assets.rs
  host_inject_at_line: 377
- id: foreign_notify_import_1
  file: breaks/break_foreign_notify_import.rs
  category: foreign_import
  hunk_start_line: 16
  hunk_end_line: 27
  rationale: 'use notify::{RecommendedWatcher, RecursiveMode, Watcher} +
    notify::recommended_watcher() setting up a filesystem watcher to re-render on
    change, import inside hunk. Verified foreign at SHA 78951393e29b: `notify` =
    0 grep hits across *.rs and absent from Cargo.toml; bat reads each input
    exactly once through its own InputReader (input.rs) and never watches the
    filesystem.'
  difficulty: easy
  host_file: src/controller.rs
  host_inject_at_line: 29
- id: foreign_tracing_import_1
  file: breaks/break_foreign_tracing_import.rs
  category: foreign_import
  hunk_start_line: 18
  hunk_end_line: 26
  rationale: 'use tracing::instrument + tracing::info_span! / tracing::info!
    spans wrapped around the highlight loop, import inside hunk. Verified foreign
    at SHA 78951393e29b: `tracing` = 0 grep hits across *.rs and absent from
    Cargo.toml; bat surfaces diagnostics through its own bat_warning! macro
    (macros.rs) writing to stderr, never a tracing subscriber.'
  difficulty: easy
  host_file: src/printer.rs
  host_inject_at_line: 43
- id: foreign_sqlx_api_1
  file: breaks/break_foreign_sqlx_api.rs
  category: foreign_api
  hunk_start_line: 14
  hunk_end_line: 25
  rationale: 'sqlx::postgres::PgPoolOptions::new() + sqlx::query().bind().execute()
    async-logging per-render output metrics to Postgres, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA 78951393e29b:
    `sqlx` = 0 grep hits across *.rs and absent from Cargo.toml; bat''s output
    layer (output.rs) writes only to a terminal handle or the bundled `minus`
    pager, never to a database.'
  difficulty: easy
  host_file: src/output.rs
  host_inject_at_line: 65
- id: foreign_diesel_api_1
  file: breaks/break_foreign_diesel_api.rs
  category: foreign_api
  hunk_start_line: 14
  hunk_end_line: 20
  rationale: 'diesel::pg::PgConnection::establish() + diesel::sql_query().load()
    loading user configuration rows from Postgres, referenced by fully-qualified
    path (no `use` import). Verified foreign at SHA 78951393e29b: `diesel` = 0
    grep hits across *.rs and absent from Cargo.toml; bat builds its Config from
    CLI args and a local config file (config.rs, bin/bat/config.rs), never from
    a SQL database.'
  difficulty: easy
  host_file: src/config.rs
  host_inject_at_line: 124
- id: foreign_chrono_api_1
  file: breaks/break_foreign_chrono_api.rs
  category: foreign_api
  hunk_start_line: 15
  hunk_end_line: 19
  rationale: 'chrono::Utc::now() + chrono::Duration::seconds() rendered as a
    gutter timestamp decoration, referenced by fully-qualified path (no `use`
    import). Verified foreign at SHA 78951393e29b: `chrono` = 0 grep hits across
    *.rs and absent from Cargo.toml; bat''s decorations are line numbers,
    git-change markers and grid borders (decorations.rs) computed from the input
    alone, with no clock or date dependency.'
  difficulty: medium
  host_file: src/decorations.rs
  host_inject_at_line: 72
- id: foreign_tokio_concurrency_1
  file: breaks/break_foreign_tokio_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 14
  hunk_end_line: 27
  rationale: 'tokio::runtime::Runtime::new().block_on() + tokio::spawn() spinning
    up an async runtime to drive preprocessing concurrently, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA 78951393e29b:
    `tokio` = 0 grep hits across *.rs and absent from Cargo.toml; bat''s
    preprocessing (expand_tabs / sanitize / replace_nonprintable in
    preprocessor.rs) is entirely synchronous, with no async runtime in the crate.'
  difficulty: easy
  host_file: src/preprocessor.rs
  host_inject_at_line: 139
- id: foreign_rayon_concurrency_1
  file: breaks/break_foreign_rayon_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 22
  hunk_end_line: 30
  rationale: 'use rayon::prelude::* + par_iter() data-parallel filtering of line
    numbers against configured ranges, import inside hunk. Verified foreign at
    SHA 78951393e29b: `rayon` = 0 grep hits across *.rs and absent from
    Cargo.toml; bat evaluates line ranges with a plain sequential itertools-based
    pass (line_range.rs) and has no parallelism.'
  difficulty: easy
  host_file: src/line_range.rs
  host_inject_at_line: 28
- id: foreign_crossbeam_concurrency_1
  file: breaks/break_foreign_crossbeam_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 19
  hunk_end_line: 32
  rationale: 'crossbeam::channel::unbounded() + crossbeam::thread::scope() fanning
    style-component resolution across worker threads, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA 78951393e29b:
    `crossbeam` = 0 grep hits across *.rs and absent from Cargo.toml; bat
    resolves its StyleComponents synchronously in a single pass (style.rs) and
    its only threading is the paging thread spawned via std::thread in output.rs.'
  difficulty: medium
  host_file: src/style.rs
  host_inject_at_line: 88
- id: foreign_rand_import_1
  file: breaks/break_foreign_rand_import.rs
  category: foreign_import
  hunk_start_line: 14
  hunk_end_line: 20
  rationale: 'use rand::Rng + rand::thread_rng().gen_range() picking a theme at
    random from the embedded set, import inside hunk. Verified foreign at SHA
    78951393e29b: `rand` = 0 grep hits across *.rs and absent from Cargo.toml;
    bat resolves the theme deterministically from config/env
    (HighlightingAssets::get_theme in assets.rs), never at random.'
  difficulty: easy
  host_file: src/assets.rs
  host_inject_at_line: 377
- id: foreign_crossterm_import_1
  file: breaks/break_foreign_crossterm_import.rs
  category: foreign_import
  hunk_start_line: 13
  hunk_end_line: 20
  rationale: 'use crossterm::terminal + terminal::size() sizing the pager''s wrap
    width, import inside hunk. Verified foreign at SHA 78951393e29b: `crossterm`
    = 0 grep hits across *.rs and absent from Cargo.toml; bat measures the
    terminal through `console` / `terminal-colorsaurus` and grep-cli, never
    crossterm.'
  difficulty: easy
  host_file: src/pager.rs
  host_inject_at_line: 19
- id: foreign_dirs_import_1
  file: breaks/break_foreign_dirs_import.rs
  category: foreign_import
  hunk_start_line: 17
  hunk_end_line: 21
  rationale: 'ALIASED import `use dirs as user_dirs` + user_dirs::config_dir(),
    the alias masking the crate name (import stage still keys off the `dirs`
    crate root). Verified foreign at SHA 78951393e29b: `dirs` = 0 grep hits
    across *.rs and absent from Cargo.toml; bat resolves cache/config dirs
    through `etcetera` (BaseStrategy in bin/bat/directories.rs) plus the
    BAT_CACHE_PATH / BAT_CONFIG_DIR env vars, never the `dirs` crate.'
  difficulty: medium
  host_file: src/bin/bat/directories.rs
  host_inject_at_line: 49
- id: foreign_zstd_import_1
  file: breaks/break_foreign_zstd_import.rs
  category: foreign_import
  hunk_start_line: 14
  hunk_end_line: 21
  rationale: 'SUBMODULE import `use zstd::stream::read::Decoder` + Decoder::new()
    decompressing a theme blob. Verified foreign at SHA 78951393e29b: `zstd` = 0
    grep hits across *.rs and absent from Cargo.toml; bat decompresses its
    embedded syntax/theme assets with `flate2` (COMPRESS_THEMES / from_binary in
    assets.rs), never zstd.'
  difficulty: medium
  host_file: src/assets.rs
  host_inject_at_line: 380
- id: foreign_git2_api_1
  file: breaks/break_foreign_git2_api.rs
  category: foreign_api
  hunk_start_line: 16
  hunk_end_line: 20
  rationale: 'git2::Repository::open() + git2::Repository::statuses() reading
    worktree changes, referenced by fully-qualified path (no `use` import).
    Verified foreign at SHA 78951393e29b: `git2` = 0 grep hits across *.rs and
    absent from Cargo.toml; bat computes line modifications through `gix`
    (gix::diff::blob in diff.rs, the `git` feature), never the libgit2 `git2`
    crate.'
  difficulty: easy
  host_file: src/diff.rs
  host_inject_at_line: 19
- id: foreign_uuid_api_1
  file: breaks/break_foreign_uuid_api.rs
  category: foreign_api
  hunk_start_line: 14
  hunk_end_line: 17
  rationale: 'uuid::Uuid::new_v4() stamping each render with a session tag,
    referenced by fully-qualified path (no `use` import). Verified foreign at
    SHA 78951393e29b: `uuid` = 0 grep hits across *.rs and absent from
    Cargo.toml; bat identifies inputs by their ordinal/filename in InputReader
    (input.rs) and never mints UUIDs.'
  difficulty: easy
  host_file: src/printer.rs
  host_inject_at_line: 43
- id: foreign_redis_api_1
  file: breaks/break_foreign_redis_api.rs
  category: foreign_api
  hunk_start_line: 15
  hunk_end_line: 19
  rationale: 'redis client caching serialized theme sets — the foreign namespace
    is named once on the constructor (redis::Client::open), then writes flow
    through a local `client`/`con` RECEIVER variable. Verified foreign at SHA
    78951393e29b: `redis` = 0 grep hits across *.rs and absent from Cargo.toml;
    bat caches assets on the local filesystem (asset_from_cache in assets.rs),
    never in an external key-value store.'
  difficulty: medium
  host_file: src/assets.rs
  host_inject_at_line: 381
- id: foreign_lru_api_1
  file: breaks/break_foreign_lru_api.rs
  category: foreign_api
  hunk_start_line: 17
  hunk_end_line: 23
  rationale: 'HARD — an lru::LruCache reached ONLY through a receiver whose
    methods (contains, get, cloned) collide with bat''s own attested map idioms,
    the crate named only in the parameter TYPE position (no `use`, no
    `foreign::` callee). Verified foreign at SHA 78951393e29b: `lru` = 0 grep
    hits across *.rs and absent from Cargo.toml; bat caches parsed configuration
    in plain owned structs (config.rs), not an LRU cache. Genuinely hard: the
    foreignness lives in a type annotation the import/call-receiver stages do
    not inspect.'
  difficulty: hard
  host_file: src/config.rs
  host_inject_at_line: 124
- id: foreign_sled_api_1
  file: breaks/break_foreign_sled_api.rs
  category: foreign_api
  hunk_start_line: 17
  hunk_end_line: 21
  rationale: 'HARD — a sled::Db embedded key-value store read ONLY through a
    receiver whose methods (get, ok, map, to_vec) are all attested bat idioms,
    the crate named only in the parameter TYPE position (no `use`, no
    `foreign::` callee). Verified foreign at SHA 78951393e29b: `sled` = 0 grep
    hits across *.rs and absent from Cargo.toml; bat''s asset cache is a
    directory of bincode blobs (asset_from_cache in assets.rs), never an
    embedded database. Genuinely hard: the only foreign token sits in a type
    annotation neither stage inspects.'
  difficulty: hard
  host_file: src/assets.rs
  host_inject_at_line: 377
- id: foreign_threadpool_concurrency_1
  file: breaks/break_foreign_threadpool_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 14
  hunk_end_line: 25
  rationale: 'threadpool::ThreadPool::new() + pool.execute() fanning line-range
    membership checks across a foreign thread pool, referenced by
    fully-qualified path (no `use` import). Verified foreign at SHA
    78951393e29b: `threadpool` = 0 grep hits across *.rs and absent from
    Cargo.toml; bat evaluates line ranges with a single sequential itertools
    pass (line_range.rs) and has no thread pool.'
  difficulty: easy
  host_file: src/line_range.rs
  host_inject_at_line: 28
- id: foreign_asyncstd_concurrency_1
  file: breaks/break_foreign_asyncstd_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 15
  hunk_end_line: 24
  rationale: 'async-std runtime driving preprocessing on an async task, reached
    through a SUBMODULE import `use async_std::task` + task::block_on(). Verified
    foreign at SHA 78951393e29b: `async_std` = 0 grep hits across *.rs and absent
    from Cargo.toml; bat''s preprocessing (expand_tabs / replace_nonprintable in
    preprocessor.rs) is entirely synchronous, with no async runtime in the
    crate.'
  difficulty: medium
  host_file: src/preprocessor.rs
  host_inject_at_line: 139
- id: foreign_flume_concurrency_1
  file: breaks/break_foreign_flume_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 14
  hunk_end_line: 19
  rationale: 'flume MPMC channel wiring a writer thread, reached through an
    ALIASED import `use flume as chan` + chan::unbounded(), the alias masking the
    crate name (import stage still keys off the `flume` crate root). Verified
    foreign at SHA 78951393e29b: `flume` = 0 grep hits across *.rs and absent
    from Cargo.toml; bat''s only cross-thread handoff is the paging thread
    spawned via std::thread in output.rs, with no external channel crate.'
  difficulty: medium
  host_file: src/output.rs
  host_inject_at_line: 58
- id: foreign_dashmap_concurrency_1
  file: breaks/break_foreign_dashmap_concurrency.rs
  category: foreign_concurrency
  class: foreign_import
  hunk_start_line: 22
  hunk_end_line: 32
  rationale: 'HARD — a dashmap::DashMap concurrent map tallying style components,
    reached ONLY through a receiver whose methods (get, insert) mimic bat''s own
    single-threaded HashMap idiom, the crate named only in the parameter TYPE
    position (no `use`, no `foreign::` callee). Verified foreign at SHA
    78951393e29b: `dashmap` = 0 grep hits across *.rs and absent from Cargo.toml;
    bat resolves StyleComponents synchronously over a plain HashSet (style.rs),
    with no concurrent map. Genuinely hard: the foreign type is indistinguishable
    from a std map at the call site.'
  difficulty: hard
  host_file: src/style.rs
  host_inject_at_line: 88
