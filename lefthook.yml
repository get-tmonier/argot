pre-commit:
  parallel: true
  commands:
    rustfmt:
      glob: "crates/**/*.rs"
      run: cargo fmt --check
    clippy:
      glob: "crates/**/*.rs"
      run: cargo clippy --workspace --all-targets -- -D warnings
