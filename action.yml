# Composite GitHub Action for argot — voice linter that learns a repo's voice
# from git history. See https://argot.tmonier.com/docs/ci/ for copy-paste workflows.
#
# It is advisory by default: argot is a statistical guardrail, so it informs the
# PR with a visual score and inline annotations without gating the merge. The
# reviewer has the last word (opt into `fail-on-hits` for a hard gate).
#
# What it does:
#   1. Downloads the prebuilt `argot` binary from this repo's GitHub Releases
#      (cargo-dist artifacts: argot-{target}.tar.xz).
#   2. Restores/caches the `.argot/` model directory keyed on the tracked
#      source files and the argot version; runs `argot fit` on a cache miss.
#   3. Runs `argot check --format <format>` and writes the document to a file.
#   4. Renders a non-blocking "voice score" card into the Actions job summary,
#      and (on pull requests) into a single sticky PR comment.
#   5. Optionally uploads the SARIF file to GitHub code scanning.
#
# Supported runners: Linux (x64/arm64), macOS (arm64/x64), and Windows (x64) —
# the targets argot publishes prebuilt binaries for.
#
# Note: checking a ref range (the default on pull requests) needs git history.
# Use `actions/checkout` with `fetch-depth: 0`.

name: "argot"
description: "Score a PR against the repo's learned voice — a non-blocking visual score card, PR comment, and SARIF code-scanning annotations."
branding:
  icon: "mic"
  color: "purple"

inputs:
  path:
    description: "Path to the repository to check (working-directory relative)."
    required: false
    default: "."
  argot-version:
    description: "argot release to install: 'latest' or a version like '0.2.43'."
    required: false
    default: "latest"
  format:
    description: "Output format for `argot check`: sarif, json, or human."
    required: false
    default: "sarif"
  output-file:
    description: "File the check results are written to."
    required: false
    default: "argot-results.sarif"
  ref:
    description: >-
      Git ref or range to check (e.g. 'origin/main..HEAD'). Empty = automatic:
      base..HEAD on pull requests, the head commit on pushes.
    required: false
    default: ""
  cache:
    description: "Cache the fitted .argot/ model directory between runs."
    required: false
    default: "true"
  upload-sarif:
    description: "Upload the SARIF file to GitHub code scanning (requires format: sarif and `security-events: write`)."
    required: false
    default: "true"
  comment-pr:
    description: "Post/update a non-blocking voice-score comment on the pull request (requires `pull-requests: write`)."
    required: false
    default: "true"
  fail-on-hits:
    description: >-
      Fail the job when argot finds hunks above the calibrated threshold.
      Off by default: argot is a statistical guardrail, so it informs the PR
      (score card + code-scanning annotations) without gating the merge. Opt in
      only if you deliberately want a hard gate.
    required: false
    default: "false"

outputs:
  exit-code:
    description: "Exit code of `argot check` (0 clean, 1 hits found)."
    value: ${{ steps.check.outputs.exit-code }}
  results-file:
    description: "Path to the written results file."
    value: ${{ steps.check.outputs.results-file }}

runs:
  using: "composite"
  steps:
    - name: Install argot
      shell: bash
      env:
        ARGOT_VERSION: ${{ inputs.argot-version }}
      run: |
        set -euo pipefail
        # Unix ships .tar.xz; Windows ships .zip with argot.exe.
        case "${RUNNER_OS}-${RUNNER_ARCH}" in
          Linux-X64)   target="x86_64-unknown-linux-gnu"; ext="tar.xz"; bin="argot" ;;
          Linux-ARM64) target="aarch64-unknown-linux-gnu"; ext="tar.xz"; bin="argot" ;;
          macOS-ARM64) target="aarch64-apple-darwin"; ext="tar.xz"; bin="argot" ;;
          macOS-X64)   target="x86_64-apple-darwin"; ext="tar.xz"; bin="argot" ;;
          Windows-X64) target="x86_64-pc-windows-msvc"; ext="zip"; bin="argot.exe" ;;
          *)
            echo "::error::argot publishes prebuilt binaries for Linux (x64/arm64), macOS (arm64/x64), and Windows (x64); got ${RUNNER_OS}-${RUNNER_ARCH}"
            exit 1
            ;;
        esac
        if [ "$ARGOT_VERSION" = "latest" ]; then
          url="https://github.com/get-tmonier/argot/releases/latest/download/argot-${target}.${ext}"
        else
          url="https://github.com/get-tmonier/argot/releases/download/v${ARGOT_VERSION#v}/argot-${target}.${ext}"
        fi
        install_dir="${RUNNER_TEMP}/argot-bin"
        mkdir -p "$install_dir"
        archive="${RUNNER_TEMP}/argot.${ext}"
        echo "Downloading $url"
        curl --proto '=https' --tlsv1.2 -fsSL --retry 3 "$url" -o "$archive"
        # bsdtar (the `tar` on GitHub's Windows runners) extracts .zip too, so
        # one code path serves both archive kinds.
        tar -xf "$archive" -C "$install_dir" --strip-components=1
        echo "$install_dir" >> "$GITHUB_PATH"
        "$install_dir/$bin" --version

    - name: Cache argot model
      if: ${{ inputs.cache == 'true' }}
      id: model-cache
      uses: actions/cache@v4
      with:
        path: ${{ inputs.path }}/.argot
        key: argot-model-${{ runner.os }}-${{ inputs.argot-version }}-${{ hashFiles('**/*.py', '**/*.ts', '**/*.tsx', '**/*.js', '**/*.jsx') }}

    - name: Fit voice model
      if: ${{ inputs.cache != 'true' || steps.model-cache.outputs.cache-hit != 'true' }}
      shell: bash
      env:
        ARGOT_PATH: ${{ inputs.path }}
      run: |
        set -euo pipefail
        argot fit --repo "$ARGOT_PATH"

    - name: Run argot check
      id: check
      shell: bash
      env:
        ARGOT_PATH: ${{ inputs.path }}
        ARGOT_REF: ${{ inputs.ref }}
        ARGOT_FORMAT: ${{ inputs.format }}
        ARGOT_OUT: ${{ inputs.output-file }}
      run: |
        set -euo pipefail
        args=()
        if [ -n "$ARGOT_REF" ]; then
          args+=("$ARGOT_REF")
        elif [ -n "${GITHUB_BASE_REF:-}" ]; then
          # Pull request: check everything on the branch vs the base.
          args+=("origin/${GITHUB_BASE_REF}..HEAD")
        else
          # Push: check the head commit.
          args+=(--commit HEAD)
        fi
        set +e
        argot check --repo "$ARGOT_PATH" "${args[@]}" --format "$ARGOT_FORMAT" > "$ARGOT_OUT"
        code=$?
        set -e
        echo "exit-code=$code" >> "$GITHUB_OUTPUT"
        echo "results-file=$ARGOT_OUT" >> "$GITHUB_OUTPUT"
        if [ "$code" -ge 2 ]; then
          echo "::error::argot check failed (exit $code)"
          cat "$ARGOT_OUT" || true
          exit "$code"
        fi
        if [ "$code" -eq 1 ]; then
          echo "argot found hunks above the calibrated threshold (see $ARGOT_OUT)."
        else
          echo "argot check clean."
        fi

    - name: Voice score card
      id: card
      shell: bash
      env:
        ARGOT_PATH: ${{ inputs.path }}
        ARGOT_REF: ${{ inputs.ref }}
      run: |
        set -euo pipefail
        # The range to summarize: explicit ref wins, else base..HEAD on PRs,
        # else the head commit vs its parent on pushes.
        if [ -n "$ARGOT_REF" ]; then
          range="$ARGOT_REF"
        elif [ -n "${GITHUB_BASE_REF:-}" ]; then
          range="origin/${GITHUB_BASE_REF}..HEAD"
        elif git -C "$ARGOT_PATH" rev-parse --verify -q HEAD~1 >/dev/null; then
          range="HEAD~1..HEAD"
        else
          range=""
        fi
        if [ -z "$range" ]; then
          echo "No diff range to score — skipping the voice card."
          echo "card=" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        card="${RUNNER_TEMP}/argot-voice-card.md"
        # Advisory: never let card rendering fail the job.
        if argot voice-diff "$range" --repo "$ARGOT_PATH" --format markdown > "$card" 2>/dev/null; then
          # Rich score card in the Actions run UI — no extra permissions, fork-safe.
          cat "$card" >> "$GITHUB_STEP_SUMMARY"
          echo "card=$card" >> "$GITHUB_OUTPUT"
        else
          echo "voice-diff could not score '$range' (is the model fitted?) — skipping card."
          echo "card=" >> "$GITHUB_OUTPUT"
        fi

    - name: Comment voice score on the PR
      if: ${{ inputs.comment-pr == 'true' && github.event_name == 'pull_request' && steps.card.outputs.card != '' }}
      uses: actions/github-script@v7
      env:
        ARGOT_CARD: ${{ steps.card.outputs.card }}
      with:
        github-token: ${{ github.token }}
        script: |
          const fs = require('fs');
          const marker = '<!-- argot-voice-check -->';
          const body = marker + '\n' + fs.readFileSync(process.env.ARGOT_CARD, 'utf8');
          const { owner, repo } = context.repo;
          const issue_number = context.payload.pull_request.number;
          // Sticky comment: update the one we already posted, or create it once.
          const comments = await github.paginate(github.rest.issues.listComments, {
            owner, repo, issue_number,
          });
          const existing = comments.find((c) => c.body && c.body.includes(marker));
          if (existing) {
            await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
          } else {
            await github.rest.issues.createComment({ owner, repo, issue_number, body });
          }

    - name: Upload SARIF to code scanning
      if: ${{ inputs.upload-sarif == 'true' && inputs.format == 'sarif' }}
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: ${{ inputs.output-file }}
        category: argot

    - name: Fail on hits
      if: ${{ inputs.fail-on-hits == 'true' && steps.check.outputs.exit-code == '1' }}
      shell: bash
      run: |
        echo "::error::argot found hunks above the calibrated threshold — see the uploaded results."
        exit 1
