{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "D=\"${CLAUDE_PROJECT_DIR:-.}\"; [ -f \"$D/.argot/scorer-config.json\" ] && command -v argot >/dev/null 2>&1 && argot hook --repo \"$D\" || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
