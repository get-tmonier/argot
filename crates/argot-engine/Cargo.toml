[package]
name = "argot-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
# Internal crate: shipped only inside the `argot` binary, never published to
# crates.io (path deps forbid it). Marked non-publishable so it cannot leak.
publish = false
description = "Rule-agnostic guardrail engine: the detector contract, check orchestration, config, suppression surfaces, and git/corpus walking — no rule-group logic, no cargo features."

[dependencies]
argot-lang = { path = "../argot-lang" }
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
thiserror.workspace = true
git2.workspace = true
sha2.workspace = true
toml.workspace = true
toml_edit.workspace = true

[dev-dependencies]
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true
